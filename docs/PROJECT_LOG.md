# Project Log

Use this file to capture decisions, open questions, and next steps so we can resume smoothly across sessions.

## Summary
- Multi-agent PyGame swarm environment with stigmergy.
- PettingZoo Parallel API for dict-based multi-agent interactions.
- Discrete 9-action joystick interface.
- DQN training with independent or shared policies.
- Optional SB3/RLlib DQN training backends.
- Demo and screenshot tooling included.
 - RLlib backend uses compatibility shims and may require setting `RAY_TMPDIR` if `/tmp` is full.

## Change Log
- Logged a Q&A pointing to the current reward source-of-truth locations: reward constants in `env/config.py`, reward application paths in `env/swarm_env.py`, and per-episode reward-breakdown logging in `train/independent_dqn_pytorch.py`.
- Added a repo rule: when Codex updates files, it must log a short summary in `docs/PROJECT_LOG.md` (added to `AGENTS.md`).
- Summary of recent work: converted `SwarmEnv` to PettingZoo Parallel API, updated training/demo scripts to dict-based I/O, added SB3/RLlib backends with dispatcher, expanded dependencies in `requirements.txt`, updated docs, and added RLlib compatibility shims; checkpoints saved for RLlib in `checkpoints/rllib_dqn/`.
- Fixed RLlib demo loading by normalizing the checkpoint path to a `file://` URI in `train/demo.py`.
- Patched RLlib demo to coerce replay buffer type to a string during `Algorithm.from_checkpoint`.
- Registered the `swarm_pz` env in RLlib demo before loading checkpoints.
- Disabled strict RLlib DQN config validation during demo checkpoint load to avoid replay buffer type errors.
- Added `--max-steps` to `train/demo.py` so demos can auto-exit for smoke tests.
- Added `--ray-tmpdir` flag to RLlib training and demo to avoid `/tmp` pressure and socket path length issues.
- Added `.ray_tmp/` to `.gitignore`.
- Added `docs/ONBOARDING.md` with a code-first RL walkthrough for future developers.
- Expanded `docs/ONBOARDING.md` with the DQN trainer mapping (theory → code and key blocks).
- Added `docs/QandA.md` to capture common questions and answers.
- Added an `AGENTS.md` rule to log system/codebase questions into `docs/QandA.md`.
- Added an Environment MDP diagram to `docs/UML.md`.
- Logged a Q&A about `_handle_targets` rewards and sim-to-real implications.
- Logged a Q&A clarifying that target radius checks imply environment target knowledge in sim.
- Added `docs/ToDo.md` with a sim-to-real required-change checklist.
- Logged Q&A about what it means for the env to be PettingZoo-native.
- Logged Q&A defining MLP (Multi-Layer Perceptron).
- Added inline comments to `train/independent_dqn_pytorch.py` and expanded the DQN walkthrough in `docs/ONBOARDING.md`.
- Logged Q&A about SB3/RLlib training vs underlying DL libraries.
- Logged Q&A about PettingZoo vs SB3/RLlib vs PyTorch roles.
- Logged Q&A explaining that a Q-function is not itself a policy.
- Logged Q&A clarifying that `batch_size`/`lr` are optimizer settings while `epsilon_*` is exploration.
- Expanded `docs/ToDo.md` with sim-to-real training checks and logged the related Q&A.
- Added line-by-line annotations to `train/independent_dqn_pytorch.py` and noted this in `docs/ONBOARDING.md`.
- Logged Q&A defining what "PettingZoo Parallel" means.
- Added `docs/RESOURCES.md` with official links (PettingZoo/Gymnasium), GitHub repos, and learning resources.
- Added Stanford CS234 YouTube playlist to `docs/RESOURCES.md`.
- Logged beginner learning path Q&A in `docs/QandA.md`.
- Logged a Q&A summarizing how to learn the repo by following the environment, custom DQN trainer, backend dispatcher, and demo flow.
- Logged a beginner-oriented Q&A mapping core RL concepts directly onto the main environment and DQN trainer files.
- Logged a Q&A describing a minimal-change Raspberry Pi + Arduino sim-to-real architecture with a Pi-side real-world adapter and Arduino low-level control layer.
- Added `docs/SimToReal.md` with a detailed Raspberry Pi + Arduino deployment design and added a `robot/` package containing `SensorBridge`, `ObservationBuilder`, `PolicyRunner`, `ActionBridge`, and a runtime loop skeleton.
- Logged a Q&A summarizing `QNetwork`, the Bellman update, and one full DQN training iteration in the custom trainer.
- Added `docs/DQN_EXPLAINED.md` to capture the custom DQN trainer explanation in one place and logged a Q&A pointing to it.
- Logged a Q&A comparing the `custom`, `sb3`, and `rllib` training backends and when to choose each one.
- Logged a Q&A clarifying that the sim-to-real policy loop can mirror the custom demo inference loop, with sensor-built observations and a real-world action bridge replacing `env.step(...)`.
- Expanded `docs/SimToReal.md` to map the `robot/` package onto the custom demo inference loop and logged a Q&A confirming that `robot/` is the Pi-side sim-to-real deployment skeleton.
- Logged a Q&A covering the tradeoff between sharing only the checkpoint plus a minimal inference snippet versus using this repo’s `robot/` package.
- Logged a Q&A clarifying the actual minimal Pi-side runtime dependencies: `swarm_env.py` is not needed, but the current `robot/` package still depends on `env/config.py` and `train/independent_dqn_pytorch.py` for `QNetwork`.
- Logged a Q&A confirming that the current `ant.py` / `ant.service` setup looks like Raspberry Pi-side runtime code, based on its Python script, systemd service, and serial communication with an ESP32.
- Added a `pi/` reference integration layer based on the existing Raspberry Pi control structure, showing how Raspberry Pi code can reuse `robot/`, `models/q_network.py`, and `env/config.py` to run the trained policy on the physical robot.
- Logged a Q&A summarizing what `ant.py` currently does: serial connection, scan reading, handcrafted free-space action selection, and ESP32 command transmission.
- Added `pi/ants.py` as a preserved Raspberry Pi control script matching the current rule-based behavior and added `docs/PI_MIGRATION.md` to guide gradual migration from that baseline toward model-based control.
- Added `pi/ants.service` as a systemd unit mirroring the current Raspberry Pi deployment pattern and documented the path-adjustment requirement in `docs/PI_MIGRATION.md`.
- Expanded `docs/PI_MIGRATION.md` to state explicitly that `pi/run_policy.py` is the intended migrated end state, while `pi/ants.py` remains the baseline, and logged the matching Q&A.
- Extracted `QNetwork` into `models/q_network.py` and updated training, demo, and robot inference code to share it, removing the robot runtime's dependency on the training script.
- Upgraded the environment for science-fair-grade stigmergic foraging support by adding configurable nest mechanics, food pickup and nest delivery, richer local observations (nest direction, food presence, carrying state), nest rendering, and aligned the robot observation builder to the new observation contract.
- Upgraded the custom training pipeline for research-grade experiments by adding structured episode/evaluation logging, checkpoint metadata, optional deterministic evaluation during training, training plot generation, and standalone evaluation/plot scripts while preserving the existing DQN CLI flow.
- Added a reusable experiment framework on top of the existing trainer/evaluator, including shared env sweep flags, benchmark experiment definitions, a central experiment runner, aggregated trial metrics with mean/std, and publication-style summary plots under the existing `runs/`, `results/`, and `analysis/` structure.

## Key Commands
- Random rollout: `python train/random_rollout.py`
- Train (headless): `python train/independent_dqn_pytorch.py --headless --total-steps 10000 --save-dir checkpoints --save-every 2000`
- Demo: `python train/demo.py --checkpoint-dir checkpoints`
- Screenshot gallery: `python train/capture_screenshots.py`

## Decisions
- Default training uses independent Q-networks unless `--shared-policy` is passed.
- Pheromone grid uses simple deposit + decay + diffusion.

## Open Questions
- Do we want a nest/return task and food-carrying state?
- Should pheromone deposit depend on carrying food?
- Add video capture or logging dashboards?

## Next Steps
- Add carry state + nest reward (optional)
- Add two pheromone channels (food vs nest)
- Improve training stability or add PPO alternative

## 2026-03-18
- Added `collective_intelligence_scaling` to the shared experiment registry with agent-count sweeps for pheromone-on vs pheromone-off conditions and a default 20-trial setup.
- Extended the shared experiment runner to honor per-experiment default trial counts, carry case metadata into trial/aggregate CSVs, and derive `efficiency_per_robot` only in the aggregation layer.
- Extended analysis plotting with grouped error-bar plots and generated collective-scaling outputs for completion time, swarm efficiency, pheromone usage, and efficiency per robot versus swarm size.
- Added a rule-based swarm baseline policy that consumes the standard 23-dim observation and outputs the same 9 discrete actions used by the learned controllers.
- Extended the shared evaluation pipeline and experiment runner to support `rl_algorithm_comparison`, including DQN, shared-policy DQN, and rule-based cases under the same eval/output schema.
- Added analysis-layer derived metrics for `convergence_speed` and `time_to_first_food` plus algorithm-comparison plots for food retrieval, efficiency, convergence speed, and overall efficiency bars.
- Refreshed the top-level architecture and onboarding docs to match the current 23-dim environment, experiment framework, and baseline set.
- Added a focused `docs/manual/` set covering quick start, project structure, experiments, and results interpretation for new users and judges.
- Logged the backward-compatibility assessment for commit `8ac33636556db0af11cd7c09b9b57e03d94a40d1`, noting that action semantics remain stable but the 19-dim observation/checkpoint contract no longer matches the current 23-dim environment.
- Logged the exact changed-file list relative to revision `8ac33636556db0af11cd7c09b9b57e03d94a40d1`.
- Added `docs/API_REFERENCE.md`, a code-grounded developer API/specification reference covering the environment contract, 23-dim observation layout, 9-action mapping, dynamics, rewards, config fields, and training/evaluation/experiment entry points.
- Added focused companion specs `docs/OBSERVATION_SPEC.md` and `docs/ACTION_SPEC.md` so developers can reference the current observation and action contracts without reading the full API reference.
- Added `docs/CONFIG_REFERENCE.md` and `docs/EXPERIMENT_API.md` to split the config surface and experiment framework into focused developer references.
- Added `train/policy_probe.py`, a beginner-friendly single-file script for manually feeding hand-written 23-dim observation vectors into a trained custom DQN checkpoint and inspecting Q-values, chosen action, and a plain-language interpretation.
- Updated onboarding, API reference, and quick-start docs to include how to run `train/policy_probe.py`.
- Fixed `train/policy_probe.py` so it prints observations using the active checkpoint layout and can explain both current 23-D and legacy 19-D checkpoint inputs by inspecting the checkpoint tensor shapes instead of assuming a single observation format.
- Logged a Q&A clarifying that `--headless` disables PyGame rendering and screenshot capture but does not change environment stepping, observations, rewards, or training logic.
- Logged a Q&A explaining why saving checkpoints during training is useful for resuming runs, comparing intermediate policies, running demos/evaluations later, and avoiding loss of progress.
- Logged a Q&A clarifying that `--save-dir` defaults to `checkpoints/` and that the trainer still writes a final checkpoint there even when `--save-every` is left at `0`.
- Logged a Q&A walking through the execution flow of `train/independent_dqn_pytorch.py`, including env setup, epsilon-greedy action selection, replay storage, Bellman updates, target-network sync, evaluation, logging, and checkpoint saving.
- Logged a Q&A explaining that `train/evaluate.py` runs headless deterministic evaluation for DQN or rule-based policies, writes per-episode metrics to `eval_metrics.csv`, and writes aggregated means to `eval_summary.json`.
- Added `docs/TRAINING_CUSTOM.md`, a detailed implementation-grounded walkthrough of the custom DQN trainer, including the main training flow, a line-by-line Bellman update explanation, and a tensor-shape walkthrough of one training step.
- Added `docs/EVALUATION_CUSTOM.md`, a detailed implementation-grounded walkthrough of `train/evaluate.py`, covering policy loading, deterministic evaluation flow, metric logging, summary generation, and the main differences from training.
- Added detailed explanatory comments throughout `train/policy_probe.py` without changing its behavior, clarifying observation layouts, legacy checkpoint handling, checkpoint-shape inspection, the one-step forward pass, and the plain-language action explanation logic.
- Added `docs/CAMERA_PROPOSAL.md` describing how to use an onboard camera as a feature-producing perception sensor, how to simulate camera-derived features in the environment, and a phased observation-contract and training plan for integrating vision without jumping straight to raw-image RL.
- Logged a Q&A explaining what each observation value in `train/policy_probe.py` represents, including lidar, target/nest/neighbor body-frame vectors, heading encoding, normalized speed, food/carry state, pheromone samples, and the legacy 19-D checkpoint difference.
- Logged a Q&A clarifying how to convert normalized observation values back into sim units and then into millimeters by applying `lidar_max_range`, `max_speed`, and an external mm-per-sim-unit calibration.
- Logged a Q&A recommending `1 sim unit = 10 mm` as a practical starting calibration, with rationale based on robot size, lidar range, world size, and the current Pi-side millimeter command conventions.
- Added `firmware/run.py`, a firmware-side trained-policy runner that reuses the existing sensor/action adapters and commands the robot through `firmware/ant.py`.
- Updated `firmware/run.py` to bypass `ESP32ActionBridge` and send actions directly through `ESP32Robot.turn()`, `move()`, and `stop()`.
- Updated `pi/run_policy.py` to remove the `pi.esp32_*` bridge modules and interact directly with `firmware/ant.py` for both scan reads and motion commands.
- Logged a Q&A noting that `firmware/run.py` compiles, but a CLI smoke test is currently blocked by a missing `pyserial` dependency in `firmware/ant.py`.
- Inlined the policy config, scan bucketization, observation building, checkpoint loading, and action prediction logic inside `firmware/run.py` so it no longer imports `env.config`, `pi.esp32_sensor_adapter`, `robot.observation_builder`, or `robot.policy_runner`.
- Logged a Q&A documenting the current scale mapping in `firmware/run.py`, including lidar normalization and direct motion-command distances/turn angles.
- Logged a Q&A clarifying that `firmware/run.py` treats TOF `-1` readings as out-of-range/far by leaving the corresponding lidar bucket at its default max-range value.
- Logged a Q&A explaining that `firmware/run.py` uses explicit CLI mm/degree parameters rather than a hidden sim-unit conversion, and noted which arguments control those scales.
- Logged a Q&A computing the simulator calibration for a 70 mm radius robot: `1 sim unit = 7 mm` if `agent_radius` remains `10`.
- Logged a Q&A pointing to where the sim-unit calibration is defined and documented (`env/config.py` and `docs/QandA.md`).
- Logged a Q&A confirming that `agent_radius = 7` and `target_radius = 3` are consistent under a `1 sim unit = 10 mm` calibration, with the note that target radius should be chosen to match the physical target and pickup tolerance.
- Logged a Q&A with the deployment steps for copying checkpoints to the Pi, installing dependencies including `pyserial`, running `firmware/run.py`, and updating the systemd service if needed.
- Logged a Q&A auditing current `pi/` and `robot/` usage, noting that only part of `robot/` is still used by `pi/run_policy.py` while several `pi/esp32_*` and `robot/` integration modules are currently orphaned.
- Logged a Q&A confirming that removing `pi/` and `robot/` is safe if `firmware/run.py` is the only deployment path, with the caveat that docs and any `pi/run_policy.py` usage must be updated too.
- Logged a Q&A recommending `firmware/` as the better current robot runtime path when simplicity and direct deployment matter more than preserving the older `pi/` and `robot/` abstraction layers.
- Removed the `pi/` and `robot/` packages, added `pyserial` to `requirements.txt`, and updated the live docs to make `firmware/` the single documented robot deployment path.
- Logged a Q&A clarifying that `firmware/run.py` expects `--checkpoint-dir` to be a directory containing `shared.pt` or `agent_0.pt`, not a direct file path.
- Logged a Q&A clarifying that `firmware/run.py` still requires PyTorch for runtime inference because checkpoint loading and the forward pass both use `torch` and `models/q_network.py`.
- Added `firmware/requirements.txt`, a Raspberry Pi deployment-focused dependency list containing only the current robot runtime requirements instead of the full training stack.
- Added a `--debug` mode to `firmware/run.py` that prints startup settings, checkpoint path, per-step scan/action summaries, Q-values, lidar previews, and shutdown messages for easier hardware-side debugging.
- Added `firmware/serialmonitor.py`, a minimal timestamped serial console for `/dev/ttyUSB0` at `115200` baud with CLI overrides for port, baudrate, timeout, and startup delay.
- Logged a Q&A clarifying that there is no `agent0.py` entrypoint, that `checkpoints/agent_0.pt` is a model checkpoint, and that the current runtime entrypoints are `train/*.py` for simulation and `firmware/run.py` for robot inference.
- Logged a Q&A clarifying how to use `checkpoints/agent_0.pt`: via `firmware/run.py` for robot inference or `train/demo.py` for simulator playback, and noted that the runner expects a checkpoint directory rather than a direct `.pt` path.
- Logged a Q&A clarifying that `agent_0.pt`, `agent_1.pt`, and similar files are per-agent checkpoints for distinct swarm agents, while `shared.pt` is the shared-policy alternative rather than a versioned successor.
- Logged a Q&A pointing to the reward definitions in `env/config.py`, the reward application flow in `env/swarm_env.py`, and the per-episode reward breakdown logging in `train/independent_dqn_pytorch.py`.
- Logged a Q&A summarizing the current training commands: the custom DQN entrypoint in `train/independent_dqn_pytorch.py`, optional checkpoint-save flags, and the `train/train.py` dispatcher for SB3 and RLlib backends.
- Logged a Q&A explaining that `mission_control/comms` talks to each robot over its own line-oriented TCP or serial connection, with newline-delimited protocol messages carrying `robot_id` and replies returning on the same connection.
- Logged a Q&A recommending the current training-compatible pheromone awareness radius: three forward samples out to `4.5 * agent_radius` (currently `31.5 cm` with `agent_radius = 7`), and noting that changing it should be paired with retraining and matching updates in both simulator and command center.
- Logged a Q&A recommending a shared pheromone-data structure: keep the mutable field authoritative in `mission_control/`, expose a small shared contract for config/query/deposit/response data, and have `firmware/` consume sampled values rather than maintain its own competing field state.
- Logged a Q&A clarifying that the current training setup uses pheromone as part of the observation and environment dynamics by default, while explicit pheromone reward shaping remains disabled because `reward_pheromone_following` defaults to `0.0`.
- Logged a Q&A summarizing each agent's training input: the current 23-D local observation vector assembled in `env/swarm_env.py` and consumed by the DQN training path in `train/independent_dqn_pytorch.py`.
- Logged a Q&A clarifying how to compute nest-relative `x`/`y` displacement for `firmware/run.py`: maintain local `(x, y, theta)` state, apply turn before translation to match `execute_action(...)`, and update position with `x += d * cos(theta)`, `y += d * sin(theta)`.
- Logged a Q&A clarifying that the current setup is stochastic during training because of epsilon-greedy action selection and replay sampling, deterministic at inference because policies use greedy `argmax`, and only environment-stochastic when reset randomness or hover/mixed dynamics noise are active.
- Logged a Q&A clarifying that the deployed robot runtime in `firmware/run.py` is policy-deterministic because it selects actions with `torch.argmax`, with any real-world variability coming from observations or hardware rather than action sampling.
- Logged a Q&A clarifying that making the deployed robot policy stochastic is mechanically easy without changing the observation/checkpoint/action interfaces, for example by using epsilon-greedy inference, softmax sampling over Q-values, or adding small noise before `argmax`.
- Audited and cleaned `docs/QandA.md` to remove stale historical entries, delete references to removed `pi/` and `robot/` paths, replace outdated `firmware/` references with the current `firmware/` paths, and keep only repo-current questions that still matter.
- Restored `docs/QandA.md` to match revision `2d5293afcb7e02caca21a50fb2312411fc93bdb4` at the user’s request.
- Logged a Q&A explaining that `firmware/run.py` is the live robot-side inference loop: parse args, load checkpoint, read scans, build observations, run greedy action selection, map actions to motion commands, and send them over serial at a fixed control rate.
- Logged a Q&A noting that `firmware/run.py` was first added on 2026-03-21 in commit `e96d76ae8981201e10d5fe2a339548c8d900e425` with subject `Added saved models`.
- Logged a Q&A noting that the `models` package was created on 2026-03-18 in commit `7613e601e4e4d16896b8f91cd17b09d18cc2b2e0` (`refactor`), with `models/rule_based_policy.py` added later on 2026-03-19 in commit `b63748068e36516ba18f133606bae5dd1edc55aa`.
- Logged a Q&A clarifying that before the `models` package existed, the reusable `QNetwork` logic lived directly inside `train/independent_dqn_pytorch.py`, and the 2026-03-18 refactor extracted it into `models/q_network.py`.
- Logged a Q&A summarizing the review of the updated `docs/prompts/10_command_centre_prompt.md`: it is now much closer to the repo’s actual contracts, with remaining comments focused on precise simulator-aligned sampling geometry, heading convention, and transport/integration details.
- Logged a Q&A summarizing the review of the latest `docs/prompts/10_command_centre_prompt.md` revision: it now closely matches the repo’s pheromone contract, with remaining comments focused on the simulator’s exact normalization detail, unit-conversion clarity, and a small amount of prompt-formatting noise.
- Logged a Q&A summarizing the review of the newest `docs/prompts/10_command_centre_prompt.md` revision: it now includes exact simulator-aligned pheromone geometry, exact normalization, and an explicit default sim-to-physical unit contract, with only minor prompt-noise and operational multi-connection risks remaining.
- Logged a Q&A summarizing the review of the final cleanup revision of `docs/prompts/10_command_centre_prompt.md`: it is now effectively implementation-ready, with only cosmetic prompt-noise remaining and the main risk shifted to execution quality for multi-robot direct connections.
- Added a new isolated `mission_control/` subsystem with thread-safe world state, simulator-aligned digital pheromone field sampling, threaded TCP/serial receivers, a PyGame renderer, and a `fake_robot.py` harness for local testing without hardware.
- Added command-center docs in `docs/COMMAND_CENTER.md` and `docs/COMMAND_CENTER_FIRMWARE_PSEUDOCODE.md`, plus README and project-structure updates describing how to run the visualizer and fake robot harness.
- Added targeted explanatory comments to the non-obvious parts of the `mission_control/` package, including protocol framing, per-connection worker behavior, centered pheromone-grid mapping, simulator-matched pheromone sampling, snapshot semantics, and world-to-screen coordinate conversion.
- Logged a Q&A clarifying that the current `mission_control/` transport layer does not implement a Bluetooth-specific backend; it currently supports TCP sockets and optional serial ports only.
- Logged a Q&A describing the recommended Bluetooth design for the command center: add Bluetooth as a new line-oriented transport backend while keeping the existing ASCII protocol, world-state logic, and per-connection request/response flow unchanged.
- Added [docs/prompts/11_bluetooth_prompt.md](prompts/11_bluetooth_prompt.md), a reusable implementation prompt for adding Bluetooth transport to the command center without changing the existing protocol, pheromone semantics, or `firmware/` isolation boundaries.
- Added [docs/BLUETOOTH_FIRMWARE_NOTES.md](BLUETOOTH_FIRMWARE_NOTES.md), documenting how the current `firmware/` runtime should add a separate command-center Bluetooth client alongside `ESP32Robot`, where it fits in `run_policy.py`, and what repo-specific gaps remain around pose estimation, pheromone injection, and deposit events.
- Logged a Q&A summarizing the recommended robot-side Bluetooth structure for `firmware/`: keep `ant.py` as the local ESP32 serial link, add a separate command-center Bluetooth client, and integrate it in the observation/inference loop of `run_policy.py`.
- Clarified the Bluetooth docs to make the repo architecture explicit: in this codebase the command-center Bluetooth client belongs on the Raspberry Pi side in `firmware/`, not on the ESP32 motion/sensor controller, because `firmware/run.py` owns observation assembly and model inference.
- Logged a Q&A clarifying that `mission_control/` is the MacBook-side command-center package: it receives robot protocol messages, maintains the authoritative pheromone field and live world state, answers `SENSE` queries with `PHER_RESP`, and renders the PyGame operator UI.
- Added targeted explanatory comments to [mission_control/main.py](../mission_control/main.py) covering CLI intent, shared world-state ownership, protocol dispatch behavior, per-port serial setup, headless PyGame initialization, the main UI loop, and shutdown sequencing.
- Updated [firmware/run.py](../firmware/run.py) to maintain a simple local pose estimate from issued turn/move commands, starting at the nest/origin by default, with an optional `--initial-heading-deg` flag and debug output for `x_mm`, `y_mm`, heading, and commanded distance.
- Updated [firmware/run.py](../firmware/run.py) to support an optional BLE command-center client using a line-oriented `POS` / `SENSE` / `PHER` flow, inject returned `PHER_RESP` values into the observation vector, derive nest-relative pose from local action integration, and expose the current pheromone-awareness radius (`4.5 * agent_radius`, currently `31.5 cm` by default) in runtime settings.
- Extracted the BLE command-center transport from [firmware/run.py](../firmware/run.py) into the new helper module [firmware/bluetooth.py](../firmware/bluetooth.py), and updated `run.py` to import and use that dedicated transport file instead of embedding the BLE client class inline.
- Added targeted explanatory comments to [firmware/run.py](../firmware/run.py) around CLI grouping, simulator-compatible observation assembly, pose integration semantics, command-center BLE handoff, cm/mm conversion, and the current placeholder pheromone-deposit trigger.
- Added an optional BLE peripheral backend to [mission_control/comms/receiver.py](../mission_control/comms/receiver.py), exposed matching CLI/config flags in [mission_control/main.py](../mission_control/main.py) and [mission_control/config.py](../mission_control/config.py), documented the new transport in [docs/MISSION_CONTROL.md](MISSION_CONTROL.md), and added the optional `bless` dependency needed for BLE GATT peripheral support.
- Renamed stale `command_center` references to `mission_control` across imports, README/doc run commands, internal links, and the Pi runtime variable naming so the new subsystem name is consistent with the actual `mission_control/` package.
- Updated the Pi/Mission Control pheromone path so robots can place new pheromones over Bluetooth in normal operation: [firmware/run.py](../firmware/run.py) now enables `PHER` deposits by default with spacing based on the current awareness radius, and [mission_control/core/pheromone_field.py](../mission_control/core/pheromone_field.py) now applies each deposit as a circular field spread using the same `4.5 * agent_radius` radius rather than a single-cell write.
- Logged a Q&A clarifying that robots can now deposit pheromones in the live BLE/Mission Control runtime, with deposits triggered by the current forward-motion spacing heuristic in `firmware/run.py` rather than by an explicit learned “deposit” action.
- Logged a Q&A describing how to test `firmware/run.py`, separating syntax/import validation, Mission Control protocol checks with `mission_control/fake_robot.py`, and the real hardware run path that requires the ESP32 serial link and optional BLE Mission Control connection.
- Updated [firmware/ant.py](../firmware/ant.py) to defer annotation evaluation and raise a clear runtime error when `serial.Serial` is unavailable, and logged the matching Q&A explaining that the usual fix is installing `pyserial` in the active environment and removing the unrelated `serial` package if present.
- Logged a Q&A clarifying that learned pheromone deposition is possible in principle, but the current policy cannot learn it yet because deposition is not part of the 9-action movement policy interface and is currently implemented as runtime logic in `firmware/run.py`.
- Added more detailed explanatory comments to [firmware/run.py](../firmware/run.py) and [firmware/bluetooth.py](../firmware/bluetooth.py), covering the observation contract, scan bucketing, normalization choices, local pose semantics, control-loop stages, BLE line reconstruction, request/response matching, and failure-handling behavior.
- Logged a Q&A clarifying how Mission Control and the robot connect across BLE/TCP/serial, and noting that multi-robot support is explicit for TCP and multiple serial ports while the current BLE backend should be treated as a more limited single-link transport unless extended further.
- Extended the Mission Control BLE transport in [mission_control/comms/receiver.py](../mission_control/comms/receiver.py) to multiplex multiple robots over the shared BLE line channel by deriving logical per-robot labels from `robot_id`, and updated the Q&A to reflect that multi-robot BLE now works via protocol-level tagging rather than per-connection BLE workers.
- Logged a Q&A describing how to test the training stack incrementally: random rollout sanity check, short bounded custom DQN smoke test, checkpoint-save verification, demo playback, and optional SB3/RLlib dispatcher smoke tests.
- Logged a Q&A clarifying that the main training scripts do not render during learning because they never call `env.render()`, and that visible rendering is currently provided by `train/random_rollout.py` and `train/demo.py` instead.
- Logged a Q&A clarifying that simulator training currently deposits pheromone continuously at each agent's position on every step, with a carrying-food multiplier, rather than learning or conditionally choosing deposition events.
- Logged a Q&A clarifying that overlapping Mission Control pheromone deposits are additive, while decay still uses the same multiplicative factor; the practical effect is that stronger overlaps persist longer in absolute magnitude even though the decay rate itself does not change.
- Expanded [MISSION_CONTROL.md](MISSION_CONTROL.md) with dedicated BLE documentation covering the GATT UUIDs, robot/Mission Control message flow, digital pheromone deposits, multi-robot BLE multiplexing by `robot_id`, and the expected firmware-side runtime behavior.
- Changed the action contract from movement-only `Discrete(9)` to policy-controlled `Discrete(18)` by adding a binary pheromone-deposit choice to each movement action, introduced `reward_pheromone_deposit_cost` in [env/config.py](../env/config.py), made [env/swarm_env.py](../env/swarm_env.py) deposit pheromone only when the chosen action requests it, updated [models/rule_based_policy.py](../models/rule_based_policy.py) and [train/policy_probe.py](../train/policy_probe.py) to the new action table, and changed [firmware/run.py](../firmware/run.py) so live `PHER` messages are sent only when the policy selects a depositing action instead of by default.
- Rewrote Markdown file links across the repo from workspace-absolute `/Users/...` paths to GitHub-friendly relative paths, updated stale doc targets from old names like `AntSwarmFirmware/run_policy.py` to the current [firmware/run.py](../firmware/run.py), remapped the outdated BLE helper link to [firmware/bluetooth.py](../firmware/bluetooth.py), and removed links to deleted `pi/` and `robot/` paths where no valid GitHub target exists.
- Updated [firmware/run.py](../firmware/run.py) so the robot now transmits its Mission Control `POS` telemetry directly from the local dead-reckoned displacement estimate: it publishes the origin pose on connect, sends the current pose before each `SENSE` query, and sends the updated pose again immediately after each commanded movement step.
- Extended the Mission Control status panel to show each robot's latest transmitted dead-reckoned position and heading by adding per-robot telemetry rows in [mission_control/core/robot_registry.py](../mission_control/core/robot_registry.py) and rendering them in [mission_control/ui/panels.py](../mission_control/ui/panels.py).
- Added a short-lived control-key flash to the Mission Control panel: [mission_control/main.py](../mission_control/main.py) now records control presses, [mission_control/core/world_state.py](../mission_control/core/world_state.py) exposes active flashes for 0.3 seconds, and [mission_control/ui/panels.py](../mission_control/ui/panels.py) briefly renders the corresponding control labels in bold.
- Made the Mission Control control-key flash more pronounced in [mission_control/ui/panels.py](../mission_control/ui/panels.py) by rendering active keys with a larger bold font, a bright highlight pill, and brighter description text during the 0.3 second flash window.
- Extended the BLE/TCP robot telemetry protocol with a `LIDAR` message carrying the current 180-degree 9-bucket scan: [firmware/run.py](../firmware/run.py) now uploads the bucketized lidar ranges through [firmware/bluetooth.py](../firmware/bluetooth.py), [mission_control/comms/protocol.py](../mission_control/comms/protocol.py) parses and stores them via [mission_control/core/robot_registry.py](../mission_control/core/robot_registry.py), [mission_control/main.py](../mission_control/main.py) routes them, [mission_control/ui/panels.py](../mission_control/ui/panels.py) displays the latest values, and [mission_control/fake_robot.py](../mission_control/fake_robot.py) now emits synthetic `LIDAR` lines for local testing.
- Reworked [mission_control/fake_robot.py](../mission_control/fake_robot.py) from a fixed circular orbit into a bounded random walk with soft center steering and boundary-aware synthetic lidar, and upgraded [mission_control/ui/panels.py](../mission_control/ui/panels.py) to render each robot's 180-degree lidar as a small radar-style screen instead of just a plain numeric row.
- Fixed a Mission Control pheromone-render alignment bug in [mission_control/ui/renderer.py](../mission_control/ui/renderer.py) by flipping the pheromone grid's y-axis during heatmap drawing so deposits line up with the robot path and world-coordinate convention used by robot and trail rendering.
- Removed the mini radar-style lidar visualization from [mission_control/ui/panels.py](../mission_control/ui/panels.py) and cleaned up the unused radar colors from [mission_control/ui/colors.py](../mission_control/ui/colors.py), while keeping the plain numeric lidar readout in the status panel.
- Added a world-space 180-degree lidar semicircle to [mission_control/ui/renderer.py](../mission_control/ui/renderer.py): each robot now renders a heading-aligned max-range arc plus the latest 9 lidar rays and hit points using the current Mission Control telemetry, with new lidar overlay colors defined in [mission_control/ui/colors.py](../mission_control/ui/colors.py).
- Refined the world-space lidar overlay in [mission_control/ui/renderer.py](../mission_control/ui/renderer.py) so the semicircle is built explicitly from heading-aligned world points, closes across the diameter instead of staying open, and shows only lidar hit dots with per-dot distance labels rather than drawing the 9 ray lines; [mission_control/ui/colors.py](../mission_control/ui/colors.py) was updated to match the new label styling.
- Made the synthetic lidar in [mission_control/fake_robot.py](../mission_control/fake_robot.py) less uniform by adding bounded per-beam jitter, wobble, and occasional short-range occluder clusters, so the Mission Control lidar overlay looks more varied and realistic during local tests.
- Removed the numeric lidar distance labels from the world overlay in [mission_control/ui/renderer.py](../mission_control/ui/renderer.py) and cleaned up the now-unused label color from [mission_control/ui/colors.py](../mission_control/ui/colors.py), leaving only the semicircle and hit dots.
- Fixed repeated episode setups in the main training and demo paths by advancing the reset seed after each episode in [train/independent_dqn_pytorch.py](../train/independent_dqn_pytorch.py) and [train/demo.py](../train/demo.py), so resets now generate fresh randomized world layouts instead of replaying the same seeded setup every time.
- Exposed the epsilon-greedy exploration schedule in [train/independent_dqn_pytorch.py](../train/independent_dqn_pytorch.py) via new CLI flags for `--epsilon-start`, `--epsilon-final`, `--epsilon-decay-steps`, and `--warmup-steps`, so training can start very random and anneal toward less random behavior over time without editing code.
- Added a dedicated top-level [training_graphs/](../training_graphs/) folder and updated [train/independent_dqn_pytorch.py](../train/independent_dqn_pytorch.py) to generate matplotlib training/evaluation plots there for each run, using [train/experiment_utils.py](../train/experiment_utils.py) to write both the existing episode plots and new evaluation-step plots into a per-run graph subdirectory.
- Added a short sliding-window observation history to [env/swarm_env.py](../env/swarm_env.py) with a new `observation_history_steps` config in [env/config.py](../env/config.py), updated [models/rule_based_policy.py](../models/rule_based_policy.py) to read the newest frame from stacked observations, aligned [firmware/run.py](../firmware/run.py) with the new history-based observation contract, and documented the new default `(69,)` observation shape in [docs/OBSERVATION_SPEC.md](../docs/OBSERVATION_SPEC.md).
- Made the simulator pheromone sensing radius explicit by adding `pheromone_sample_spacing_scale` in [env/config.py](../env/config.py), routing [env/swarm_env.py](../env/swarm_env.py) through helper methods for sample distances and awareness radius, aligning [firmware/run.py](../firmware/run.py) with the same derived radius calculation, and documenting the bounded default sensing radius in [docs/OBSERVATION_SPEC.md](../docs/OBSERVATION_SPEC.md).
- Added toggleable per-agent policy debug logging via a new shared helper in [policy_debug.py](../policy_debug.py), then wired `--debug-policy`, `--debug-policy-agents`, and `--debug-policy-max-steps` into [train/demo.py](../train/demo.py), [train/evaluate.py](../train/evaluate.py), [train/independent_dqn_pytorch.py](../train/independent_dqn_pytorch.py), and [firmware/run.py](../firmware/run.py) so custom inference, evaluation, training, and robot runtime can print readable action/Q-value traces.
- Improved dense reward shaping for foraging by adding configurable `food_detection_radius`, food-approach and food-detected shaping rewards, and conservative pheromone-follow shaping in [env/config.py](../env/config.py) and [env/swarm_env.py](../env/swarm_env.py); also expanded [train/experiment_utils.py](../train/experiment_utils.py) and [train/independent_dqn_pytorch.py](../train/independent_dqn_pytorch.py) so the new shaping terms are configurable from the CLI and appear in episode reward-breakdown logs.
- Logged a Q&A summarizing the repo-level delta from commit `49ee2d9ab6388f9d660f79dfcd3d5e22a07e1b0c` to the current revision: the `server/` package was renamed to `mission_control/`, BLE/Pi-to-desktop live integration was expanded substantially, the action space grew from 9 to 18 with policy-controlled pheromone deposits, and docs/checkpoints were refreshed around that newer runtime contract.
- Logged a Q&A identifying the current merge-base split point between `main` and `model_improvements` as commit `2c9ec7085858c4712c3cd4673635cb4d3c330a45` (`2c9ec70`, `some code changes to server`), with `model_improvements` diverging after that commit.
- Logged a Q&A noting that `main` is currently 2 commits behind `model_improvements` and has no unique commits ahead of it, with the two branch-only commits being `4e3d80c` and `6a9d95d`.
- Logged a Q&A summarizing the current uncommitted working-tree changes: default stacked observation history (`69` dims from `3 x 23`), explicit pheromone sample spacing / awareness-radius math, new training/evaluation graph export into `training_graphs/`, and updated shared-policy checkpoint metadata/docs to match.
- Added a detailed Q&A entry documenting the repo evolution from commit `2c9ec7085858c4712c3cd4673635cb4d3c330a45` to `130452c`, covering the `server` -> `mission_control` rename, BLE/Mission Control and Pi-runtime expansion, the 18-action deposit-enabled contract, and the later observation-history / training-graph changes.
- Added [docs/prompts/12_additional_prompts.md](../docs/prompts/12_additional_prompts.md), a reconstructed sequence of plausible incremental prompts that could have taken the project step by step from `2c9ec70` to the current revision.
- Logged a Q&A identifying commit `49ee2d9ab6388f9d660f79dfcd3d5e22a07e1b0c` (`Mass rename`) as the version that renamed the package tree from `command_center/` to `server/`.
- Logged a detailed per-revision Q&A assessment for the post-`33cb33f4f046b9f715b0bf6b2fbcede11e0d91e0` history, including each commit’s 24-hour timestamp, touched code areas, change summary, reason, enhancement, possible risks, and whether the change direction made sense.
- Logged a Q&A clarifying each agent's simulation observation set: per-frame local features (lidar, target/nest/neighbor vectors, heading, speed, food/carry flags, pheromone), default history stacking to 69 dims, and failed-agent/noise behavior.
- Logged an architectural review Q&A for the full post-`4a0292213a1c4b87e39b2a65ab3d7ce2e300b293` evolution, concluding that the current repo is mostly coherent and loosely coupled for Mission Control/Pi integration and science-fair goals, while also noting the main weaknesses: rename churn, broad commits, and somewhat weaker research traceability than an ideal experimental codebase.
- Logged a Q&A clarifying food quantity during training: per-episode total food equals `n_targets` (default `4`), targets do not respawn within an episode, and total food across a full training run is unbounded because each episode reset spawns fresh targets.
- Logged a Q&A assessing the current repo against RL theory and implementation quality: the code is aligned with standard DQN theory and works as a practical baseline, but it remains a relatively plain value-based implementation rather than a particularly strong modern RL algorithm for a partially observable multi-agent swarm task.
- Split audit-style, history-review, and architectural-assessment Q&A out of [docs/QandA.md](QandA.md) into the new [docs/audit.md](audit.md) so `QandA.md` stays focused on understanding and using the codebase.
- Added [docs/best_rl.md](best_rl.md), a phased plan for upgrading the current DQN baseline toward a stronger existing RL stack for this problem, centered on a stronger DQN-family baseline, recurrent MAPPO as the main CTDE target, and QMIX as a cooperative comparison baseline.
- Added [docs/prompts/14_model_improvement_prompt.md](../docs/prompts/14_model_improvement_prompt.md), a future implementation prompt that turns the recommendations in [docs/best_rl.md](best_rl.md) into a concrete repo-aware request for a stronger DQN baseline, recurrent MAPPO with CTDE, and an optional QMIX comparison path.
- Tightened exploration and pheromone semantics in [env/config.py](../env/config.py), [env/swarm_env.py](../env/swarm_env.py), [train/experiment_utils.py](../train/experiment_utils.py), and [train/independent_dqn_pytorch.py](../train/independent_dqn_pytorch.py): pheromone deposition can now be gated by `pheromone_requires_food`, coverage reward is exposed explicitly as `reward_new_cell`, the environment reports `coverage_reward_total` and `pheromone_deposit_events`, and training logs now include those reward/deposit breakdowns while preserving the existing exploration reward column for compatibility.
- Added [docs/prompts/16_improve_rewards_prompt.md](../docs/prompts/16_improve_rewards_prompt.md), a repo-aware follow-up prompt that turns the current “explores but never gets first food success” diagnosis into a concrete request for curriculum/easy-mode training, food-interaction diagnostics, and minimal config-driven adjustments grounded in the existing reward and sensing code.
- Logged a Q&A clarifying that simulator pheromone is still deposited at the robots' current positions in [_update_pheromone()](../env/swarm_env.py), but with `pheromone_requires_food = True` the first deposits only appear after pickup, which makes the heatmap look like it originates at the food goal even though the grid is being written by the carrying robots.
- Reverted the default pheromone gating in [env/config.py](../env/config.py) by setting `pheromone_requires_food = False` again, so robots can deposit pheromone during exploration without requiring prior food pickup.
- Logged a Q&A clarifying that the training console's `food` number in [train/independent_dqn_pytorch.py](../train/independent_dqn_pytorch.py) is counting `food_delivered`, not food discovery or pickup, which can make training look like it never finds food even when [train/demo.py](../train/demo.py) visibly reaches and picks up targets.
- Logged a Q&A identifying why simulator pheromone appears to expand without stopping: [_update_pheromone()](../env/swarm_env.py) diffuses with wraparound `np.roll(...)` neighbors and never clips tiny residual values, so with ongoing deposits and slow decay the heatmap keeps spreading across the whole grid.
- Added [docs/prompts/16_fix_bugs.md](../docs/prompts/16_fix_bugs.md), a repo-aware implementation prompt covering two concrete fixes: bounded-edge pheromone diffusion with residual clipping, and clearer training/evaluation food metrics that distinguish pickups from deliveries so demo and training no longer appear inconsistent.
- Fixed two concrete simulator/training issues across [env/config.py](../env/config.py), [env/swarm_env.py](../env/swarm_env.py), [train/independent_dqn_pytorch.py](../train/independent_dqn_pytorch.py), [train/evaluate.py](../train/evaluate.py), and [train/experiment_utils.py](../train/experiment_utils.py): pheromone diffusion no longer wraps around the arena edges and now clips tiny residual values, while training/evaluation logs, CSVs, summaries, and plots now distinguish food pickup/discovery from food delivery instead of printing delivery counts under an ambiguous `food` label.
- Logged a Q&A clarifying that simulator pheromone visually “pops out” because deposition happens onto discrete grid cells after movement in [_update_pheromone()](../env/swarm_env.py), and [_draw_pheromone()](../env/swarm_env.py) renders normalized heatmap rectangles rather than a continuous droplet trail.
- Logged a Q&A pointing to the concrete custom DQN implementation files: [train/independent_dqn_pytorch.py](../train/independent_dqn_pytorch.py) for replay, epsilon-greedy action selection, target-network Bellman updates, and checkpointed training, plus [models/q_network.py](../models/q_network.py) for the Q-network architecture itself.
- Removed accidental empty root-level files named after epsilon-schedule flags, replaced the ambiguous multi-line training examples in [README.md](../README.md) and [docs/ONBOARDING.md](ONBOARDING.md) with single-line commands, and logged a Q&A clarifying that files like `--epsilon-final` are shell-entry artifacts rather than outputs from [train/independent_dqn_pytorch.py](../train/independent_dqn_pytorch.py).
- Added filename-aware experiment plumbing and pheromone/fixed-horizon comparison support across [train/experiment_utils.py](../train/experiment_utils.py), [env/config.py](../env/config.py), [env/swarm_env.py](../env/swarm_env.py), [train/independent_dqn_pytorch.py](../train/independent_dqn_pytorch.py), [train/evaluate.py](../train/evaluate.py), and the new [analysis/evaluate_comparison.py](../analysis/evaluate_comparison.py): training now accepts `--filename`, preserves observation shape when pheromone is disabled, saves milestone checkpoints at 25/50/75/100 percent, evaluation can run fixed-step respawn episodes with `active_targets`, and the new comparison script writes raw/summary CSVs plus combined matplotlib plots and exploration heatmaps for the three pheromone conditions across swarm sizes.
- Logged a Q&A with concrete smoke-test and longer training commands for [train/independent_dqn_pytorch.py](../train/independent_dqn_pytorch.py) that save outputs under `checkpoints/`, including the new milestone checkpoint directories.
- Updated [train/demo.py](../train/demo.py) to accept the shared pheromone CLI flags via `add_env_config_args(...)` and to build its environment through `make_swarm_config(args)`, so demo runs now support `--use-pheromone` and `--no-use-pheromone` consistently with training and evaluation.
- Logged a Q&A identifying why demo pheromone can appear to disappear after the shared CLI refactor: the shared parser currently defaults `pheromone_requires_food` to true, so [env/swarm_env.py](../env/swarm_env.py) only deposits pheromone after pickup unless demo is run with `--no-pheromone-requires-food`.
- Logged a Q&A clarifying that training currently defaults to `pheromone_requires_food = true` through the shared CLI parser in [train/experiment_utils.py](../train/experiment_utils.py), which overrides the dataclass default in [env/config.py](../env/config.py) unless `--no-pheromone-requires-food` is passed explicitly.
- Extended the evaluation workflow in [analysis/evaluate_comparison.py](../analysis/evaluate_comparison.py) and [train/evaluate.py](../train/evaluate.py) to make `targets_collected` explicitly mean pickup events rather than deliveries, add a `targets_picked_up` raw metric, support a fourth `random_walk` no-checkpoint baseline, accept `--max-steps` as a fixed-horizon alias in the comparison runner, and include the four-condition pickup-based swarm-scaling plots plus per-condition exploration heatmaps in the experiment outputs.
- Moved the default comparison output root in [analysis/evaluate_comparison.py](../analysis/evaluate_comparison.py) from repo-root `experiment_data/` to [experiments/experiment_data/](../experiments/experiment_data/), and updated the related Q&A to reflect that `experiments/` now contains both the experiment registry source file and the default generated comparison artifacts under its `experiment_data/` subdirectory.
- Added [docs/EVALUATE.md](EVALUATE.md), a practical evaluation guide covering single-checkpoint evaluation with [train/evaluate.py](../train/evaluate.py), multi-model swarm-scaling comparison with [analysis/evaluate_comparison.py](../analysis/evaluate_comparison.py), checkpoint directory expectations, current output locations under [experiments/experiment_data/](../experiments/experiment_data/), and copy-paste commands for evaluating multiple saved models and baselines.
- Logged a Q&A clarifying that [train/evaluate.py](../train/evaluate.py) evaluates one checkpoint at a time, so comparing a pheromone-trained model against a no-pheromone-trained model with that script means running it twice with different checkpoint dirs and filenames; for automatic aggregation/plots across both, [analysis/evaluate_comparison.py](../analysis/evaluate_comparison.py) is the intended tool.
- Updated the codebase for the evaluator move from `train/` to [analysis/evaluate.py](../analysis/evaluate.py): [train/run_experiments.py](../train/run_experiments.py) now imports evaluation from the new module location, and a thin compatibility wrapper at [train/evaluate.py](../train/evaluate.py) preserves older CLI/import paths.
- Fixed the [train/evaluate.py](../train/evaluate.py) compatibility wrapper so direct execution (`python train/evaluate.py ...`) first adds the repo root to `sys.path` before importing [analysis/evaluate.py](../analysis/evaluate.py), resolving the `ModuleNotFoundError: No module named 'analysis'` failure mode.
- Logged a Q&A clarifying why evaluation can appear to stall: [analysis/evaluate.py](../analysis/evaluate.py) is silent inside the episode loop and currently forces respawn-based fixed-horizon episodes, so runs only stop at `eval_steps`/`max_steps` truncation rather than ending early when targets are exhausted.
- Logged a Q&A clarifying why `experiments/experiment_data/exploration_graphs/` can contain partial outputs while `raw/` and `graphs/` remain empty: [analysis/evaluate_comparison.py](../analysis/evaluate_comparison.py) writes exploration visuals during the long-running sweep but only writes the master CSVs and combined plots after the entire comparison completes.
- Added lightweight progress reporting to [analysis/evaluate.py](../analysis/evaluate.py) and [analysis/evaluate_comparison.py](../analysis/evaluate_comparison.py) so long evaluation runs no longer appear stuck: both evaluators now print run-start metadata, periodic step checkpoints, per-episode completion summaries, and final output summaries, with the comparison runner also reporting condition-level progress and configuration failures as they occur.
- Fixed repo-relative save path handling across [train/experiment_utils.py](../train/experiment_utils.py), [analysis/evaluate.py](../analysis/evaluate.py), [analysis/evaluate_comparison.py](../analysis/evaluate_comparison.py), [train/independent_dqn_pytorch.py](../train/independent_dqn_pytorch.py), and [train/run_experiments.py](../train/run_experiments.py): relative output, checkpoint, and save directories are now normalized against the repository root instead of depending on the shell's current working directory, and JSON writers now create parent directories before writing.
- Logged a Q&A clarifying that the “food vs agent” comparison comes from [analysis/evaluate_comparison.py](../analysis/evaluate_comparison.py) as the pickup-based `targets_collected` swarm-scaling plot and CSV summaries under [experiments/experiment_data/](../experiments/experiment_data/), rather than from the single-model outputs written by [analysis/evaluate.py](../analysis/evaluate.py).
- Logged a Q&A with concrete usage examples for running [analysis/evaluate_comparison.py](../analysis/evaluate_comparison.py), including a full 1-to-30-agent sweep and a smaller smoke-test command.
- Fixed the raw CSV export in [analysis/evaluate_comparison.py](../analysis/evaluate_comparison.py): the schema now includes `exploration_coverage`, and the writer auto-extends fieldnames for any additional per-episode metrics so final CSV writing does not crash when new columns are added to the row dict.
- Logged a Q&A clarifying why the “Time to First Discovery” comparison lines can appear disconnected: no-discovery buckets are summarized as `NaN` after excluding `-1` sentinel values, and matplotlib intentionally breaks line segments at `NaN` points.
- Added runnable training prompt files under [docs/prompts/](prompts/): [21_train_with_pheromone_prompt.md](prompts/21_train_with_pheromone_prompt.md) for saving pheromone-enabled training to `checkpoints/with_pheremone`, and [20_train_without_pheromone_prompt.md](prompts/20_train_without_pheromone_prompt.md) for saving pheromone-disabled training to `checkpoints/without_pheremone`, each with both full and smoke-test commands.
- Reorganized [analysis/evaluate_comparison.py](../analysis/evaluate_comparison.py) to produce per-condition output trees under `experiments/experiment_data/`: the runner now maps the four comparison labels to `trained_pheremone_use_pheremone`, `trained_pheremone_no_use_pheremone`, `trained_without_pheremone`, and `random_walk`, writes condition-specific raw and summary CSVs into each folder's `raw/`, saves exploration visuals into `exploration_graphs/PNG/` and `exploration_graphs/PDF/`, and copies the combined comparison plots into each condition folder's `graphs/PNG/` and `graphs/PDF/` so each directory is self-contained while the existing top-level combined outputs remain available.
- Added [docs/prompts/22_checkpoint_folder_name_prompt.md](prompts/22_checkpoint_folder_name_prompt.md), a prompt describing the training-side checkpoint layout fix: eliminate duplicate top-level `shared.pt` creation under `checkpoints/`, introduce a user-facing `--folder-name` concept for the enclosing checkpoint folder, and keep all milestone checkpoint directories nested under `checkpoints/<folder_name>/`.
- Fixed training checkpoint layout in [train/independent_dqn_pytorch.py](../train/independent_dqn_pytorch.py) and shared naming resolution in [train/experiment_utils.py](../train/experiment_utils.py): training now prefers `--folder-name`, resolves one enclosing checkpoint folder under the base `--save-dir`, writes root checkpoint metadata to `checkpoints/<folder_name>/metadata.json`, keeps milestone checkpoints nested under that folder, and redirects periodic/final saves to `checkpoints/<folder_name>/full_policy/` so the old duplicate top-level `checkpoints/shared.pt` path is no longer created. Updated the main training examples in [README.md](../README.md) and [docs/ONBOARDING.md](ONBOARDING.md) to show the new folder-based checkpoint layout.
- Logged a Q&A clarifying why even a smoke comparison run can feel slow: [analysis/evaluate_comparison.py](../analysis/evaluate_comparison.py) still executes a four-condition nested sweep over every requested agent count and step horizon, then also generates CSVs, exploration visuals, and copied graph outputs for each condition.
- Removed the per-condition output-folder generation from [analysis/evaluate_comparison.py](../analysis/evaluate_comparison.py) and restored a single shared output layout under the chosen output root: the runner now writes only `raw/`, `graphs/PNG/`, `graphs/PDF/`, and `exploration_graphs/`, without creating condition-specific subdirectories like `trained_pheremone_use_pheremone/` or copying graph assets into them.
- Logged a Q&A clarifying why `pheromone_vs_random_time_to_first_discovery_vs_agents` can have missing points: buckets with no valid discovery events aggregate to `NaN`, and matplotlib intentionally omits those points and connecting segments.
- Logged a Q&A clarifying that the vertical “candlestick” shapes on the comparison plots are standard-deviation error bars around the mean metric value for each condition and agent count, not financial candlesticks.
- Removed the visible standard-deviation error bars from the comparison figures in [analysis/evaluate_comparison.py](../analysis/evaluate_comparison.py): the plots now render as plain mean-value lines with markers, while the underlying summary CSV still retains the `std_*` columns for reference.
- Organized exploration visuals in [analysis/evaluate_comparison.py](../analysis/evaluate_comparison.py) into `exploration_graphs/PNG/` and `exploration_graphs/PDF/` under the shared output root, and updated [docs/EVALUATE.md](EVALUATE.md) plus the related Q&A entries to reflect the split PNG/PDF exploration layout.
- Optimized the shared-policy path in [train/independent_dqn_pytorch.py](../train/independent_dqn_pytorch.py): replay sampling now returns NumPy arrays and converts with `torch.as_tensor(...)` at the device boundary, shared-policy action selection now uses one batched forward pass across all agents, shared-policy learning now concatenates minibatches across ready agent buffers into one optimizer step, and shared-policy evaluation also uses batched inference. Verified with `python -m py_compile train/independent_dqn_pytorch.py` and a short `.venv/bin/python train/independent_dqn_pytorch.py --headless --shared-policy --total-steps 200 --warmup-steps 10 --eval-every 0 --save-every 0 --no-plots --output-dir /tmp/sim_train_runs --save-dir /tmp/sim_train_ckpts --folder-name perf_smoke` smoke run.
- Logged a Q&A clarifying that [analysis/evaluate_comparison.py](../analysis/evaluate_comparison.py) is slowed partly by the same per-agent inference pattern seen in the old shared-policy trainer, but more importantly by its full nested sweep over conditions, swarm sizes, episodes, CSV export, exploration-image generation, and final comparison plots.
- Logged a Q&A clarifying that [train/demo.py](../train/demo.py) does not load checkpoints by `--filename` directly; to run a specific saved training name, you pass `--checkpoint-dir checkpoints/<folder_name>/full_policy`, with `--shared-policy` when the checkpoint is shared.
- Logged a Q&A clarifying that the current food-shaping logic in [env/swarm_env.py](../env/swarm_env.py) rewards moving closer to detectable food but does not penalize moving farther away, and that a small symmetric signed-distance shaping term is the cleaner way to close that loophole.
- Updated food shaping in [env/swarm_env.py](../env/swarm_env.py) to use the same signed distance-progress term for both directions: moving closer still gives positive `reward_food_approach` shaping, while moving farther away now applies the corresponding negative shaping instead of zero, with the progress value clipped to `[-1, 1]`.
- Logged a Q&A clarifying that the signed food-approach reward in [env/swarm_env.py](../env/swarm_env.py) can still help real-world behavior as a training-only shaping signal, but sim-to-real transfer depends on whether the policy can reproduce that behavior from real sensor observations without access to simulator-only reward information.
- Logged a Q&A capturing a pheromone-dynamics review finding in [env/swarm_env.py](../env/swarm_env.py): failed agents are not currently excluded from `_update_pheromone()`, and deposit cost is charged on deposit-intent actions even when no pheromone is actually written because deposition is disabled or gated.
- Fixed both pheromone-dynamics bugs in [env/swarm_env.py](../env/swarm_env.py): `_update_pheromone()` now skips failed agents, and pheromone deposit cost is now applied only when a pheromone write actually succeeds rather than whenever the action merely requests deposition.
- Logged a Q&A clarifying that “move for a few seconds, then freeze” is more likely a policy/reward-design failure mode than a simulator bug under the current [env/config.py](../env/config.py) settings, because the action space allows zero-throttle actions and the current rewards are very sparse and heavily penalize time/collisions while offering little shaping.
- Added [docs/prompts/23_optimize_evaluation_efficiency_prompt.md](prompts/23_optimize_evaluation_efficiency_prompt.md), a repo-specific implementation prompt for speeding up [analysis/evaluate_comparison.py](../analysis/evaluate_comparison.py) by batching shared-policy inference in `_run_episode()`, reducing tensor-allocation and Python-loop overhead, preserving CSV/plot outputs, and verifying with a small comparison smoke test.
- Fixed a training-start crash caused by a config mismatch: [env/config.py](../env/config.py) now includes `pheromone_follow_min_gradient` again so [train/experiment_utils.py](../train/experiment_utils.py) can still construct `SwarmConfig(...)` with the shared CLI/env-config plumbing.
- Fixed another config mismatch crash in [env/config.py](../env/config.py): restored `reward_pheromone_following` so [_apply_pheromone_reward()](../env/swarm_env.py) can still read the field during environment stepping.
- Logged a Q&A clarifying that `--no-pheromone` in [train/demo.py](../train/demo.py) is not a real flag and is likely being parsed as `--no-pheromone-requires-food` via argparse abbreviation, which makes deposition happen more freely and therefore makes pheromone appear more prominent instead of disabling it.
- Logged a Q&A clarifying that the custom training path already supports target respawn through `--target-respawn` and `--active-targets`, because [train/independent_dqn_pytorch.py](../train/independent_dqn_pytorch.py) uses the shared env-config CLI plumbing from [train/experiment_utils.py](../train/experiment_utils.py).
- Rebalanced the default reward coefficients in [env/config.py](../env/config.py) toward a more stable foraging objective: lowered shaping magnitudes (`reward_food_approach`, `reward_food_detected`, `reward_new_cell`), strengthened `reward_nest_delivery` relative to pickup, slightly increased collision cost, kept per-step pressure gentle, and reduced pheromone deposit cost so shaping remains supportive instead of dominating the task reward.
- Added simple wall-clock ETA reporting to [train/independent_dqn_pytorch.py](../train/independent_dqn_pytorch.py): episode-end console logs now include elapsed training time and an estimated time remaining based on current steps-per-second.
- Logged a Q&A clarifying that [analysis/evaluate_comparison.py](../analysis/evaluate_comparison.py) is not fully optimized yet: `_run_episode()` still uses per-agent `torch.tensor(...)` inference rather than a batched shared-policy path, and the runner still pays the full cost of its nested sweep plus CSV/plot generation.
- Logged a Q&A clarifying that “food pickup in training but never in demo” is often expected in this repo because training episode metrics are collected under epsilon-greedy exploration, while [train/demo.py](../train/demo.py) runs the policy greedily with no epsilon; any additional env-flag mismatch between training and demo can further worsen demo performance.
- Added `--demo-epsilon` to [train/demo.py](../train/demo.py) for all backends so demo runs can include configurable random exploratory actions instead of being forced to run purely greedy.
- Logged a Q&A clarifying that training does randomize the environment across episode resets in [env/swarm_env.py](../env/swarm_env.py), but that this randomization is primarily reset-level world regeneration rather than arbitrary mid-episode world changes.
- Logged a Q&A clarifying that agents moving back and forth is usually caused by the coarse discrete action table in [env/swarm_env.py](../env/swarm_env.py), short-horizon shaping incentives, and the lack of any explicit smoothness or action-switch penalty, rather than a pure simulator malfunction.
- Logged a Q&A summarizing the current observation and action spaces from [env/swarm_env.py](../env/swarm_env.py): default observation size 69 from a 23-feature frame stacked over 3 history steps, and a discrete 18-action space from all combinations of throttle, turn, and pheromone-deposit bits.
- Changed the target-direction observation in [env/swarm_env.py](../env/swarm_env.py) from global nearest-target guidance to local-only guidance: `_nearest_target_vector()` now returns zeros when the nearest target is outside `food_detection_radius`, while the existing `food_presence` bit serves as the visibility cue. Updated [docs/OBSERVATION_SPEC.md](OBSERVATION_SPEC.md) and logged a Q&A note describing the new behavior.
- Added a fully random demo backend to [train/demo.py](../train/demo.py): `--backend random` now runs the simulator with uniformly random discrete actions using the same demo loop controls (`--max-steps`, `--headless`, seeding, and debug hooks).
- Logged a Q&A clarifying that training and demo use the same [env/swarm_env.py](../env/swarm_env.py) codepath, but [train/demo.py](../train/demo.py) does not restore training-time environment settings from checkpoint metadata, so demo can underperform badly when flags such as pheromone usage, respawn, active target count, dynamics mode, or agent count do not match the conditions the policy was trained under.
- Logged a Q&A explaining what `episode_logger` does in [train/independent_dqn_pytorch.py](../train/independent_dqn_pytorch.py): per-episode CSV metric logging, episode-end row writes, and guaranteed close/flush in the `finally` block.
- Reapplied the actual anti-jitter environment mechanics after finding the live files were missing the behavior hooks: [env/config.py](../env/config.py) now defines `action_repeat_steps` and `reward_action_switch`, [train/experiment_utils.py](../train/experiment_utils.py) exposes `--action-repeat-steps` and `--reward-action-switch`, [env/swarm_env.py](../env/swarm_env.py) now holds accepted actions for multiple ticks and penalizes executed action changes, and [train/independent_dqn_pytorch.py](../train/independent_dqn_pytorch.py) now records `reward_action_switch_component` in episode metrics.
- Added [docs/prompts/23_recurrent_mappo_curriculum_prompt.md](prompts/23_recurrent_mappo_curriculum_prompt.md), a repo-specific implementation prompt for a GRU-based recurrent MAPPO training path with curriculum learning across `1 agent -> small swarm (2-5) -> full MARL (5+)`, while treating the environment’s randomized resets as built-in domain randomization.
- Added an initial runnable recurrent MAPPO curriculum path: [train/mappo_gru.py](../train/mappo_gru.py) now provides a GRU-based shared-actor MAPPO-style trainer with a centralized recurrent critic, PPO-style updates, curriculum stages over `1 agent -> small swarm -> full swarm`, checkpoint/resume support, and per-stage metadata; [algorithms/mappo/networks.py](../algorithms/mappo/networks.py), [algorithms/mappo/curriculum.py](../algorithms/mappo/curriculum.py), and [algorithms/mappo/inference.py](../algorithms/mappo/inference.py) provide the supporting algorithm modules; [train/train.py](../train/train.py) now dispatches `--backend mappo`; and [docs/TRAINING_MAPPO.md](TRAINING_MAPPO.md) documents the new path. Logged a Q&A note explaining the current critic-reset limitation across curriculum stages when centralized state size changes with swarm size.
- Logged a Q&A documenting how to run the new recurrent MAPPO trainer through [train/train.py](../train/train.py) using `--backend mappo`, including example commands for `stage1`, `stage1_to_2`, `full`, and checkpoint resume.
- Logged a Q&A documenting how to test the recurrent MAPPO path: short `stage1` and `stage1_to_2` smoke runs through [train/train.py](../train/train.py), checkpoint/output verification under `checkpoints/` and `runs/`, and resume-path verification using `--resume-checkpoint`, while noting that a dedicated MAPPO demo/evaluation path is not implemented yet.
- Added MAPPO demo support to [train/demo.py](../train/demo.py): the demo entrypoint now supports `--backend mappo`, loads recurrent actor checkpoints via [algorithms/mappo/inference.py](../algorithms/mappo/inference.py), maintains per-agent GRU hidden state across steps, resets hidden state on episode reset, and supports headless demo runs for MAPPO checkpoints. Updated [docs/QandA.md](QandA.md) with the new command pattern.
- Added lightweight runtime progress prints to [train/mappo_gru.py](../train/mappo_gru.py): the recurrent MAPPO trainer now prints stage-start summaries, episode-end progress lines with reward/food/coverage/pheromone/length plus elapsed time and ETA, evaluation summaries when `--eval-every` is enabled, and stage-completion summaries, all outside the hot inner step loop to keep overhead negligible.
- Logged a Q&A explaining why the current recurrent MAPPO path in [train/mappo_gru.py](../train/mappo_gru.py) runs noticeably faster than the custom DQN trainer in [train/independent_dqn_pytorch.py](../train/independent_dqn_pytorch.py): MAPPO uses shared batched on-policy rollouts and consolidated updates, while the DQN path still pays more replay-buffer, target-network, epsilon-greedy, and Python-side experiment-management overhead.
- Fixed a MAPPO food-metrics bug in [train/mappo_gru.py](../train/mappo_gru.py): the trainer and evaluator now track `targets_collected` separately from `food_delivered` instead of incorrectly writing delivery counts into all food-related fields, so MAPPO logs and runtime prints can distinguish pickups from completed nest deliveries. Logged a Q&A clarifying that `full` curriculum also gives only about one third of `--total-steps` to the final 6-agent stage, so zero deliveries can still reflect undertraining even after the metrics fix.
- Fixed a MAPPO eval CSV schema bug in [train/mappo_gru.py](../train/mappo_gru.py): `eval_metrics.csv` now includes the `food_picked_up` column so the evaluator can log the newly separated pickup-vs-delivery metrics without crashing the CSV writer.
- Hardened the shared plotting helpers in [train/experiment_utils.py](../train/experiment_utils.py): training and evaluation metric plots now sort x-values, drop non-finite points, and collapse duplicate x-indices before plotting, which fixes broken or disconnected line segments in graphs such as the first pickup plot when runs contain repeated or partially written rows.
- Logged a Q&A clarifying that agents moving briefly and then stopping is usually a policy/reward-design failure mode in the current setup rather than a simulator freeze: the action space in [env/swarm_env.py](../env/swarm_env.py) includes zero-throttle actions, the environment now holds actions for multiple ticks, and [env/config.py](../env/config.py) currently combines time/collision/action-switch penalties with sparse task rewards and no nest-delivery reward by default.
- Fixed the new random-demo path in [train/demo.py](../train/demo.py): restored the missing `--headless` CLI argument, passed `headless=args.headless` into `SwarmEnv(...)`, default-bounded headless runs to one episode horizon when `--max-steps` is omitted, and guarded the custom/SB3/RLlib/random demo loops so they only poll Pygame events and render frames when not running headless.
- Logged a Q&A clarifying the current observation space definition and source of truth: [env/swarm_env.py](../env/swarm_env.py) defines a default per-agent `Box` observation of shape `(69,)`, assembled from 23 features per frame over 3 stacked history frames, with the detailed breakdown also documented in [docs/OBSERVATION_SPEC.md](OBSERVATION_SPEC.md).
- Logged a Q&A clarifying the current action space definition and source of truth: [env/swarm_env.py](../env/swarm_env.py) defines a per-agent `Discrete(cfg.num_actions)` action space, and `_build_action_table()` currently enumerates the 18 combinations of throttle, turn, and pheromone-deposit request bits from the defaults in [env/config.py](../env/config.py).
- Upgraded target observability in [env/swarm_env.py](../env/swarm_env.py): nearest-target features are now exposed only when the target is both within `cfg.lidar_max_range` and unobstructed by obstacles, `food_presence` and detectable-food shaping now use the same line-of-sight-aware criterion, and the target observation block now includes normalized distance and relative angle in addition to body-frame `dx/dy`, increasing the default observation size from 69 to 75. Updated [docs/OBSERVATION_SPEC.md](OBSERVATION_SPEC.md) and refreshed the related entries in [docs/QandA.md](QandA.md).
- Logged a Q&A clarifying the post-change observation contract: the default observation space is now a 75-D per-agent Box built from 25 features per frame over 3 stacked frames, with the target block expanded to detectable `dx`, `dy`, distance, and angle under lidar-range line-of-sight gating.
- Logged a Q&A clarifying why the target block currently uses 4 features: `dx/dy` carry local Cartesian direction, while explicit distance/angle expose range and bearing directly for easier learning, even though that makes the block partly redundant and not strictly necessary.
- Simplified the target observation block in [env/swarm_env.py](../env/swarm_env.py) from four features down to just normalized detectable target distance and relative angle, restoring the default observation shape to 23 features per frame and 69 after 3-frame stacking while keeping the lidar-range and line-of-sight gating. Updated [docs/OBSERVATION_SPEC.md](OBSERVATION_SPEC.md) and the related Q&A entries in [docs/QandA.md](QandA.md).
- Logged a Q&A clarifying the meaning of the `food_presence` observation in [env/swarm_env.py](../env/swarm_env.py): it is a local binary cue that becomes `1.0` only when a target is currently detectable under the same lidar-range and obstacle-blocking rule used by the target distance/angle features.
- Tightened target detectability in [env/swarm_env.py](../env/swarm_env.py): `_target_visible()` now also requires the target to lie within the agent’s front-facing 180 degrees, so target distance/angle, `food_presence`, and detectable-food shaping all ignore targets behind the agent. Updated [docs/OBSERVATION_SPEC.md](OBSERVATION_SPEC.md) and [docs/QandA.md](QandA.md).
- Logged a Q&A restating the current action space from [env/swarm_env.py](../env/swarm_env.py): a per-agent `Discrete(18)` built from all combinations of throttle, turn, and pheromone-deposit request bits.
- Added two anti-jitter controls to the environment and shared CLI config: [env/swarm_env.py](../env/swarm_env.py) now holds each accepted discrete action for `cfg.action_repeat_steps` ticks and applies a small `cfg.reward_action_switch` penalty when the executed action changes; [train/experiment_utils.py](../train/experiment_utils.py) now exposes these as `--action-repeat-steps` and `--reward-action-switch`. Logged the behavior in [docs/QandA.md](QandA.md).
- Restored pure random demo support in [train/demo.py](../train/demo.py): the CLI once again accepts `--backend random`, and the demo now includes a dedicated `_random_demo(...)` loop that samples actions uniformly, respects `--headless`, `--max-steps`, and deterministic reset seeding, and resets cleanly between episodes.
- Logged a Q&A confirming that prompt 24 is implemented for the recurrent MAPPO path: [algorithms/mappo/curriculum.py](../algorithms/mappo/curriculum.py) now defines six stage-specific environment profiles from tiny single-agent to full hard-swarm, and [train/mappo_gru.py](../train/mappo_gru.py) applies those profiles to env construction and records the staged env settings in run metadata, CSV logs, runtime prints, and checkpoint metadata.
- Logged a Q&A documenting how to run the different curriculum slices for recurrent MAPPO through [train/train.py](../train/train.py): `--curriculum stage1` for the two single-agent stages, `--curriculum stage1_to_2` for single-agent plus small-swarm stages, and `--curriculum full` for all six stages up to the final hard full-swarm environment.
- Logged a Q&A documenting how to test trained MAPPO checkpoints through [train/demo.py](../train/demo.py): use `--backend mappo` with `--checkpoint-dir` pointed at a stage folder or `latest`, and set `--n-agents` to match the stage checkpoint being demoed.
- Logged a Q&A clarifying why the `stage3b_full_swarm_final` MAPPO checkpoint can appear to just spin in demo: [train/demo.py](../train/demo.py) does not currently restore stage-specific environment settings from checkpoint metadata, and the example command used `--n-agents 10` even though the checkpoint metadata records `n_agents = 6`, so the actor was being tested under a mismatched swarm size and world profile.
- Logged a follow-up Q&A clarifying why the stage3b MAPPO checkpoint can still fail even after switching demo to `--n-agents 6`: the current demo path still cannot reproduce the trained stage world exactly because width/height are not exposed in the CLI and stage metadata is not auto-applied, so environment mismatch remains; beyond that, the first-pass MAPPO policy itself may still simply be weak.
- Logged a Q&A clarifying that “stuck in place while spinning” is still fully consistent with a learned policy in the current env: [env/swarm_env.py](../env/swarm_env.py) includes explicit zero-throttle turn actions, and the tank dynamics apply yaw even when forward speed is zero, so repeated selection of those actions yields turn-in-place behavior without requiring any simulator bug.
- Logged a Q&A clarifying that the MAPPO curriculum stage definitions live in [algorithms/mappo/curriculum.py](../algorithms/mappo/curriculum.py), while [train/mappo_gru.py](../train/mappo_gru.py) is the file that applies those stages to environment construction and the training loop.
- Updated [train/demo.py](../train/demo.py) so MAPPO stage checkpoints automatically apply their stored stage environment metadata by default: when `metadata.json` is present, demo now rebuilds the stage world using the saved `n_agents`, `n_targets`, `n_obstacles`, `max_steps`, `active_targets`, `target_respawn`, and `width`/`height`, which makes checkpoints such as `stage3b_full_swarm_final` default to the intended large final curriculum environment instead of the generic demo defaults. Logged a Q&A describing the new behavior.
- Resolved merge-conflict markers in [docs/QandA.md](QandA.md) by keeping a single coherent Q&A branch and removing duplicated conflict content.
- Updated [train/train.py](../train/train.py) so the top-level dispatcher now accepts explicit `--use-pheromone` / `--no-use-pheromone` flags and forwards the selected toggle into backend-specific argument parsing.
- Logged a Q&A clarifying that [analysis/evaluate_comparison.py](../analysis/evaluate_comparison.py) is not yet at full efficiency: `_run_episode()` still uses per-agent tensor construction and per-agent forward passes in the inner loop, alongside the script's inherently expensive nested sweep and plotting/CSV output workload.
- Added MAPPO GRU policy support to both evaluators: [analysis/evaluate.py](../analysis/evaluate.py) now accepts `--policy-kind mappo_gru` and runs recurrent actor inference with per-episode hidden-state reset/masking, and [analysis/evaluate_comparison.py](../analysis/evaluate_comparison.py) now accepts `--policy-kind {dqn,mappo_gru}`, loads `actor.pt` checkpoints, and evaluates comparison conditions using recurrent MAPPO inference when selected.
- Added [docs/curriculum_training.md](curriculum_training.md), a detailed walkthrough of training in this repo covering the top-level backend dispatcher, the general custom DQN training flow, the general recurrent MAPPO flow, and the current curriculum-learning implementation with Mermaid diagrams for stage scheduling, weight transfer, and checkpoint flow.
- Logged a Q&A explaining the online Q-network vs target Q-network distinction in the DQN trainer and why both are needed for stable Bellman targets.
- Logged a Q&A explaining how environment placement works in [env/swarm_env.py](../env/swarm_env.py): reset-time ordering for obstacles, nest, targets, and agents; obstacle rectangle generation; and the shared `_sample_free_position()` rejection rules that avoid overlaps with obstacles, nest clearance, targets, and agents.
- Added [docs/prompts/24_curriculum_build_env_prompt.md](prompts/24_curriculum_build_env_prompt.md), a repo-specific implementation prompt for stage-specific curriculum environments that progress from an extremely easy single-agent world to a very large, obstacle-heavy full-swarm final environment, while preserving within-stage randomization and the existing MAPPO curriculum path.
- Implemented stage-specific curriculum environments for the recurrent MAPPO path. [algorithms/mappo/curriculum.py](../algorithms/mappo/curriculum.py) now defines six explicit stage profiles (`stage1a_single_agent_tiny` through `stage3b_full_swarm_final`) with staged swarm size, world size, target count, obstacle count, horizon, and active-target settings. [train/mappo_gru.py](../train/mappo_gru.py) now builds each stage environment from those profiles, records stage env settings in run metadata and stage checkpoints, logs stage environment dimensions in episode/eval CSVs, and prints them at runtime. Updated [docs/TRAINING_MAPPO.md](TRAINING_MAPPO.md) and [docs/curriculum_training.md](curriculum_training.md) to document the six-stage easy-to-hard environment progression.
- Added [docs/trail.md](trail.md), a design note capturing the discussion of pheromone trail formation, how the desired `find target -> return to nest -> deposit pheromone -> later agents exploit trail` behavior maps onto exploration vs exploitation in RL, and why that stigmergic mechanism is useful for decentralized navigation in unknown environments such as disaster zones.
- Expanded [docs/trail.md](trail.md) with a concrete training-design discussion: how to achieve the desired trail behavior through reward structure, curriculum staging, pheromone gating, residual exploration, and the right evaluation metrics, without changing the implementation itself.
- Added [docs/prompts/25_build_trail_prompt.md](prompts/25_build_trail_prompt.md), a repo-specific implementation prompt for making the training path explicitly learn the full stigmergic loop from discovery to pickup to return-to-nest pheromone deposition to later trail exploitation, including reward priorities, pheromone gating, curriculum alignment, and trail-focused metrics/evaluation.
- Expanded [docs/prompts/25_build_trail_prompt.md](prompts/25_build_trail_prompt.md) so the requested implementation also updates `README.md` and includes copy-paste-ready example commands for training, smoke tests, demo playback, and trail-relevant evaluation/comparison runs.
- Expanded [docs/prompts/25_build_trail_prompt.md](prompts/25_build_trail_prompt.md) again so the requested implementation must also review the existing docs for stale content and update them into a coherent, current reflection of the actual system rather than only touching a few targeted files.
- Executed the trail-building prompt across the current codebase. Updated [env/config.py](../env/config.py), [train/experiment_utils.py](../train/experiment_utils.py), and [env/swarm_env.py](../env/swarm_env.py) so the defaults and env logic now emphasize the full loop from discovery to pickup to return-to-nest delivery, add carrying-food nest-progress shaping, and gate pheromone deposition to meaningful carrying-and-return behavior by default.
- Extended [train/mappo_gru.py](../train/mappo_gru.py) with trail-focused runtime metrics and prints, including pheromone deposit counts plus first-pickup / first-delivery timing and pickup-to-delivery latency in episode/eval outputs.
- Refreshed major docs to match the current system instead of older assumptions: updated [README.md](../README.md), [docs/TRAINING_MAPPO.md](TRAINING_MAPPO.md), [docs/ARCHITECTURE.md](ARCHITECTURE.md), [docs/CONFIG_REFERENCE.md](CONFIG_REFERENCE.md), [docs/API_REFERENCE.md](API_REFERENCE.md), [docs/EVALUATE.md](EVALUATE.md), [docs/ONBOARDING.md](ONBOARDING.md), [docs/manual/QUICK_START.md](manual/QUICK_START.md), [docs/SimToReal.md](SimToReal.md), [docs/OBSERVATION_SPEC.md](OBSERVATION_SPEC.md), [docs/trail.md](trail.md), and [docs/QandA.md](QandA.md) so they reflect the 69-D stacked observation, 18-action space, MAPPO path, evaluator location, and trail-learning workflow.
- Increased obstacle density in the harder MAPPO curriculum stages in [algorithms/mappo/curriculum.py](../algorithms/mappo/curriculum.py): `stage2b_small_swarm_large` and `stage3a_full_swarm_large` now use 8 obstacles instead of 6, and the final `stage3b_full_swarm_final` stage now uses 18 obstacles instead of 12 so the last full-swarm world is more difficult by default.
- Updated [train/demo.py](../train/demo.py) so generic demo runs now default to `18` obstacles instead of `6` when the user does not explicitly pass `--n-obstacles`; MAPPO checkpoints with `metadata.json` still override this with their saved stage environment settings.
- Logged a Q&A clarifying git ordering around the trail prompt work: the recorded prompt-25 implementation commit is `c93c366`, the later obstacle commit is `4cc4eec`, and the current head `6a0e27a` is `restore to state of 4cc4eec`, so the restore-to-`4cc4eec` step happened after the prompt-25 implementation commit.
- Logged a Q&A confirming the current branch relationship: `build_trails` is a straight fast-forward descendant of `model_improvements` right now, with merge-base equal to `model_improvements` tip `5db8f3b` and `build_trails` 16 commits ahead with no commits unique to `model_improvements`.
- Logged a Q&A clarifying the current return-and-pheromone behavior: with the present defaults in [env/config.py](../env/config.py) and [train/experiment_utils.py](../train/experiment_utils.py), nest delivery is the main task reward and pheromone deposition is gated to agents that are already carrying food and making progress toward the nest, so deposition is intended to occur on the post-pickup return route when pheromone is enabled, though checkpoint quality still determines whether the learned policy actually performs that loop well.
- Added [docs/prompts/26_upgrade_target_delivery_prompt.md](prompts/26_upgrade_target_delivery_prompt.md), a repo-specific implementation prompt for upgrading the target-delivery loop with visible carrying-food indication, one-food-at-a-time carrying rules, return-to-nest delivery completion, return-path pheromone deposition, and repeated-use food sources with 3 total sources and 4 uses per source before respawn.
- Logged a Q&A summarizing the current observation space: the per-agent observation is still a 69-D `Box`, built from 23 features per frame and 3 stacked frames, with the current frame contents documented in [env/swarm_env.py](../env/swarm_env.py) and [docs/OBSERVATION_SPEC.md](OBSERVATION_SPEC.md).
- Executed the target-delivery upgrade prompt across the current codebase. Updated [env/config.py](../env/config.py), [train/experiment_utils.py](../train/experiment_utils.py), and [env/swarm_env.py](../env/swarm_env.py) so the default task now runs with 3 repeated-use food sources, 4 uses per source, one-food-at-a-time carrying, explicit carrying-food render state, source-use tracking, and respawn on exhaustion while preserving nest-delivery completion and return-path pheromone gating.
- Extended [train/mappo_gru.py](../train/mappo_gru.py), [train/demo.py](../train/demo.py), and [analysis/evaluate.py](../analysis/evaluate.py) so the recurrent MAPPO path, demo reconstruction, and evaluator all carry the new `food_source_capacity` setting and log source respawn / remaining-food metrics. Updated [README.md](../README.md), [docs/TRAINING_MAPPO.md](TRAINING_MAPPO.md), [docs/CONFIG_REFERENCE.md](CONFIG_REFERENCE.md), [docs/CTDE_STATE.md](CTDE_STATE.md), and [docs/QandA.md](QandA.md) so the docs reflect the new delivery loop and repeated-use source semantics.
- Logged a Q&A clarifying how return-path pheromone deposition is actually learned in the current system: not via a separate label, but by env-side gating in [_update_pheromone()](../env/swarm_env.py) plus reward ordering in [env/config.py](../env/config.py) and nest-return shaping in [_apply_nest_return_shaping()](../env/swarm_env.py), which together make carrying-food movement toward the nest the main context where deposition can occur and be useful.
- Logged a Q&A clarifying the effect of running MAPPO with `--no-pheromone-requires-food --no-pheromone-deposit-requires-nest-progress`: the command still runs, but it disables the two gates that normally make pheromone mean “carrying food and moving home,” so deposition becomes allowed during arbitrary exploration instead of mainly on productive return paths.
- Logged a Q&A clarifying why the current `mappo_full/latest` demo agents may appear not to move: the checkpoint metadata indicates the correct large final-stage environment is being restored, so the more likely issue is weak policy quality or low-motion policy collapse under the current reward/penalty balance in the hard final environment, not a missing demo restore path.
- Added an explicit undelivered-food penalty to the task loop. [env/config.py](../env/config.py) now includes `reward_undelivered_food`, [train/experiment_utils.py](../train/experiment_utils.py) exposes it as `--reward-undelivered-food`, and [env/swarm_env.py](../env/swarm_env.py) now applies that penalty only when an episode ends with agents still carrying food, so pickup without successful nest delivery is no longer reward-neutral by default. Updated [docs/CONFIG_REFERENCE.md](CONFIG_REFERENCE.md) to document the new knob.
- Logged a Q&A clarifying why the current model may still fail to learn reliable delivery even after the delivery-loop upgrades: the delivery mechanic is implemented, but the final curriculum stage remains a hard sparse-reward problem, so likely causes are insufficient effective final-stage training, weak reward separation between pickup and delivery, or failure to learn robust return navigation in clutter.
- Logged a Q&A on how to train delivery more effectively: prioritize stronger delivery-over-pickup reward separation, use the undelivered-food penalty to make failed carry-home attempts costly, spend more effective training budget in the easier single-agent/small-swarm delivery stages, and increase total steps before judging the hard final full-swarm stage.
- Logged a Q&A clarifying why demo may appear to show no pickup indicator or repeated-use food behavior even though support exists in the code: the demo path now restores `food_source_capacity` and the env renders carrying agents plus repeated-use sources, so if those are not visible the more likely cause is that the checkpoint being demoed is weak or that `latest` points at an intermediate curriculum stage rather than a strong final policy.
- Logged a Q&A clarifying a likely visual trap in the current demo rendering: repeated-use sources are still drawn under agents, and because targets are rendered before larger agent circles, a source with remaining uses can look like it disappeared on first pickup simply because the carrying agent is occluding it.
- Fixed the demo/render visibility issue in [env/swarm_env.py](../env/swarm_env.py): repeated-use food sources are now drawn after agents so they do not appear to vanish under an overlapping agent, each source now shows simple remaining-use pips, and carrying agents now get a much stronger visual indicator including an outer halo and a carried-food marker above the body.
- Logged a Q&A clarifying that epsilon is not part of the current curriculum-learning MAPPO path: the staged curriculum in [algorithms/mappo/curriculum.py](../algorithms/mappo/curriculum.py) is used by [train/mappo_gru.py](../train/mappo_gru.py), which samples from the policy distribution during training instead of using epsilon-greedy exploration. Epsilon remains part of the separate DQN trainer.
- Logged a Q&A clarifying a practical “sufficient training” command for the current MAPPO path: because the full curriculum spreads steps across many stages and the final stage remains a large `1400x950`, `18`-obstacle, `6`-agent problem with respawning sources, a more serious starting point is several hundred thousand total steps rather than short `30k`–`180k` runs.
- Expanded that Q&A in [docs/QandA.md](QandA.md) with a flag-by-flag explanation of the recommended full MAPPO training command, covering what each argument changes in the trainer and why that particular combination is a practical strong starting point for the current repo.
- Logged a Q&A capturing a code-review-style training recommendation for prompts 25 and 26: the largest current issue is a curriculum stage-step allocation bug in [algorithms/mappo/curriculum.py](../algorithms/mappo/curriculum.py), and the strongest config suggestions after that are to strengthen delivery-over-pickup pressure, consider a harsher undelivered-food penalty, and reduce exploration bonuses once reliable delivery is the main learning target.
- Logged a Q&A comparing the serious `600k` MAPPO training command with the shorter `200k` `mappo_a` style command: the omitted trainer flags still fall back to the same defaults, so the main difference is training budget rather than algorithm settings, and `200k` is much more likely to leave the later full-swarm stages undertrained.
- Updated [README.md](../README.md) directly so the main MAPPO quickstart now shows the recommended `600k` full training command instead of the older shorter example, and added a concise flag-by-flag explanation in the README itself for the key arguments used by that full training run.
- Added [docs/prompts/27_training_improvements_prompt.md](prompts/27_training_improvements_prompt.md), a new implementation prompt based on the current code review findings. It focuses on fixing the curriculum step-allocation bug, strengthening delivery-over-pickup learning pressure, reducing late-stage overrewarding of generic exploration, refining curriculum stage progression for prompts 25 and 26, reconsidering action-repeat handling, and updating the recommended full training command and docs accordingly.
- Executed [docs/prompts/27_training_improvements_prompt.md](prompts/27_training_improvements_prompt.md). Wired stage-wise `action_repeat_steps` and `reward_new_cell` from [algorithms/mappo/curriculum.py](../algorithms/mappo/curriculum.py) into [train/mappo_gru.py](../train/mappo_gru.py) env construction and checkpoint metadata, restored `action_repeat_steps` in [train/demo.py](../train/demo.py) when reconstructing MAPPO stage environments, aligned shared defaults in [env/config.py](../env/config.py) and [train/experiment_utils.py](../train/experiment_utils.py) toward delivery (`reward_pickup = 6.0`, `reward_undelivered_food = -10.0`, `reward_new_cell = 0.01`), and updated [README.md](../README.md), [docs/TRAINING_MAPPO.md](TRAINING_MAPPO.md), [docs/CONFIG_REFERENCE.md](CONFIG_REFERENCE.md), and [docs/QandA.md](QandA.md) so the documented curriculum and recommended training command match the current system.
- Logged a Q&A with the exact MAPPO demo commands to use after the `mappo_full_600k` training run, including both the rolling `latest/` checkpoint path and the explicit `stage3b_full_swarm_final/` path.
- Logged a Q&A explaining why a MAPPO demo appeared not to honor `--max-steps 300`: the demo stop logic in [train/demo.py](../train/demo.py) is correct, but `--max-steps` defaults to `0` (“run until closed”), so if the command is started on the first line and `--max-steps 300` is typed on a later line without shell continuation, that flag is not part of the running process.
- Logged a Q&A clarifying the two demo step limits: `--max-steps` in [train/demo.py](../train/demo.py) is the outer demo-loop cap, while `max_steps` loaded from MAPPO checkpoint metadata is the environment’s per-episode horizon. So with `--max-steps 300` and stage metadata showing `max_steps=800`, the demo ends at 300 total steps, not 800.
- Logged a Q&A clarifying that `--max-steps 0` disables the outer demo-loop cap in [train/demo.py](../train/demo.py), so the demo keeps running across env resets until the window is closed or the process is interrupted.
- Logged a Q&A clarifying that the current demo path does not have a pure flag-based “never reset the env” mode: [train/demo.py](../train/demo.py) always resets on `terminated or truncated`, and [env/swarm_env.py](../env/swarm_env.py) truncates at `cfg.max_steps`, so a true no-reset demo would require code changes rather than just CLI overrides.
- Logged a Q&A explaining a likely cause of MAPPO demo agents not moving: greedy demo in [train/demo.py](../train/demo.py) exposes the learned policy directly, and in the current hard final-stage setup a checkpoint can still collapse to a conservative stop-heavy strategy, so “everyone freezes” is more likely a policy-quality issue than a broken env/render loop.
- Logged a Q&A with a practical diagnosis order for frozen MAPPO demo behavior: compare explicit stage checkpoints before blaming demo, prefer `stage3b_full_swarm_final` over `latest/` when judging final behavior, inspect late-run pickup/delivery/eval metrics to separate undertraining from reward-balance issues, and use whether earlier stages still move as the main signal for whether the problem is global or only final-stage scaling.
- Logged a Q&A summarizing the actual `mappo_full_600k` metrics after frozen demos were observed. The evaluation CSV shows zero pickups, zero deliveries, and no pheromone deposits in every stage, with rewards close to pure timeout step cost, which points to broad greedy-policy collapse rather than only final-stage failure. The log also notes that [train/mappo_gru.py](../train/mappo_gru.py) still carries only the actor across stages while rebuilding the critic each time, which likely contributes to curriculum instability.
- Added [docs/prompts/28_fix_mappo_policy_collapse_prompt.md](prompts/28_fix_mappo_policy_collapse_prompt.md), a new implementation prompt focused on the broad greedy-policy collapse seen in `mappo_full_600k`. It prioritizes critic/stage-transfer stability, stage progression based on actual learned success, greedy-eval-aligned checkpoint selection, and only then reward/control retuning.
- Logged a Q&A ranking the most likely fixes after the `mappo_full_600k` collapse: reduce critic-reset instability first, then stop advancing stages purely on elapsed steps, then select checkpoints by greedy eval quality rather than only `latest/`, and only after that revisit reward/control pressure.
- Logged a Q&A confirming that [docs/prompts/28_fix_mappo_policy_collapse_prompt.md](prompts/28_fix_mappo_policy_collapse_prompt.md) includes the full recommendation set from the collapse diagnosis, including the deeper actor/critic stage-transfer issue and the preferred fixed-dimension critic direction.
- Executed [docs/prompts/28_fix_mappo_policy_collapse_prompt.md](prompts/28_fix_mappo_policy_collapse_prompt.md). [train/mappo_gru.py](../train/mappo_gru.py) now uses a fixed padded critic-state dimension across the selected curriculum so the critic and optimizer can carry across stage boundaries, adds stage-end greedy evaluation with promotion checks and optional repeats via `--stage-repeat-limit`, and saves a `best_greedy_eval/` checkpoint for demo selection. Updated [README.md](../README.md), [docs/TRAINING_MAPPO.md](TRAINING_MAPPO.md), and [docs/QandA.md](QandA.md) so the docs explain the new anti-collapse path and recommend demoing `best_greedy_eval/`.
- Logged a Q&A summarizing the partial `mappo_full_600k_fix28` run after prompt 28. The new trainer infrastructure is working, but stage repeats and fixed critic transfer were not enough: `stage1a` still showed sampled success without greedy-eval success, `stage1b` collapsed to repeated zero-pickup / zero-delivery behavior on both attempts, and the remaining issue now looks like poor early-stage greedy-behavior formation rather than stage-transfer bookkeeping alone.
- Added [docs/prompts/29_fix_mappo_greedy_behavior_prompt.md](prompts/29_fix_mappo_greedy_behavior_prompt.md), a new implementation prompt focused on the remaining post-prompt-28 failure: reducing dependence on stochastic sampled success, retuning early-stage exploration/reward/control pressure, and making early-stage greedy pickup/delivery behavior reliable before scaling further.
- Executed [docs/prompts/29_fix_mappo_greedy_behavior_prompt.md](prompts/29_fix_mappo_greedy_behavior_prompt.md). [algorithms/mappo/curriculum.py](../algorithms/mappo/curriculum.py) now stages explicit early greedy-formation settings: stage-1 pheromone is disabled, movement penalties are softened, pickup/delivery cues are strengthened, and each stage defines `entropy_start -> entropy_end` for within-stage entropy decay. [train/mappo_gru.py](../train/mappo_gru.py) now applies those stage-specific reward/pheromone overrides, decays entropy within each stage instead of using one fixed coefficient, and prints/logs sampled-vs-greedy pickup/delivery gaps so lucky sampled behavior is easier to spot. Updated [README.md](../README.md), [docs/TRAINING_MAPPO.md](TRAINING_MAPPO.md), and [docs/QandA.md](QandA.md). Verification passed with `PYTHONPYCACHEPREFIX=/tmp/pycache python -m py_compile algorithms/mappo/curriculum.py train/mappo_gru.py` and `python train/train.py --backend mappo --headless --curriculum stage1 --n-agents 6 --total-steps 4096 --rollout-steps 64 --update-epochs 2 --minibatch-size 128 --eval-every 1024 --eval-episodes 2 --stage-repeat-limit 0 --no-plots --folder-name mappo_greedy_fix_verify`; that smoke run produced real greedy success in `stage1a_single_agent_miniscule` (`greedy_pickup = 4.0`, `greedy_delivery = 4.0`) and wrote [checkpoints/mappo_greedy_fix_verify/best_greedy_eval](../checkpoints/mappo_greedy_fix_verify/best_greedy_eval).
- Logged a Q&A summarizing the current `mappo_full_600k_fix29` artifacts after prompt 29. The new run shows that prompt 29 fixed the old early greedy-collapse problem: `stage1a_single_agent_miniscule` now has strong sampled and greedy pickup/delivery, and `stage1b`/`stage1c` have strong pickup with partial delivery. The remaining bottleneck is now much narrower and more specific: `stage1d_single_agent_delivery_obstacles` and `stage2a_small_swarm_medium` still show near-zero delivery, so the main failure has shifted to carrying-food return-to-nest completion under obstacle clutter and early swarm scaling.
- Added [docs/prompts/30_fix_mappo_return_to_nest_prompt.md](prompts/30_fix_mappo_return_to_nest_prompt.md), a new implementation prompt focused on the post-prompt-29 bottleneck. It targets carrying-food return-to-nest learning, obstacle-stage delivery robustness, carrying-state-specific shaping, and stage design changes that improve completed delivery without losing the pickup gains from prompt 29.
- Executed [docs/prompts/30_fix_mappo_return_to_nest_prompt.md](prompts/30_fix_mappo_return_to_nest_prompt.md). [env/config.py](../env/config.py) adds `carrying_reward_new_cell_scale`, and [env/swarm_env.py](../env/swarm_env.py) now applies exploration reward in a carrying-aware way so return-focused stages can suppress post-pickup wandering incentives. [algorithms/mappo/curriculum.py](../algorithms/mappo/curriculum.py) now adds `reward_nest_approach` and `carrying_reward_new_cell_scale` as staged controls, inserts `stage1d_single_agent_return_medium` before the first obstacle-return stage, keeps `stage2a_small_swarm_medium` pheromone-free, and explicitly stages return-to-nest pressure. [train/mappo_gru.py](../train/mappo_gru.py) now carries those settings into env construction and logs delivery conversion in eval/stage summaries and checkpoint metadata. Updated [README.md](../README.md), [docs/TRAINING_MAPPO.md](TRAINING_MAPPO.md), and [docs/QandA.md](QandA.md). Verification passed with `PYTHONPYCACHEPREFIX=/tmp/pycache python -m py_compile env/config.py env/swarm_env.py train/experiment_utils.py algorithms/mappo/curriculum.py train/mappo_gru.py` and `python train/train.py --backend mappo --headless --curriculum stage1 --n-agents 6 --total-steps 8192 --rollout-steps 64 --update-epochs 2 --minibatch-size 128 --eval-every 2048 --eval-episodes 2 --stage-repeat-limit 0 --no-plots --folder-name mappo_return_fix_verify3`. The smoke run completed cleanly and confirmed: `stage1a` still has strong greedy delivery, `stage1d` now gets strong greedy pickup but still zero greedy delivery, and `stage1e` now shows some sampled delivery but still zero greedy delivery. So prompt 30 improved the return-to-nest training path and narrowed the failure mode, but obstacle-stage greedy delivery is still not solved.
- 2026-03-30: Added prompt 31 in `docs/prompts/31_fix_obstacle_return_delivery_prompt.md` to target the current MAPPO bottleneck: greedy return-to-nest delivery under mild clutter, with a dedicated homing-stage refinement, simplified first obstacle-return stage, and stricter delivery-sensitive promotion criteria.
- 2026-03-30: Updated docs for prompt 31. `README.md` and `docs/TRAINING_MAPPO.md` now describe the stricter homing stage, the new `stage1e_single_agent_delivery_bridge`, the added `stage1f_single_agent_delivery_obstacles`, and the more delivery-sensitive promotion rules. Added a matching Q&A entry to `docs/QandA.md`.
- 2026-03-30: Executed prompt 31. Updated `algorithms/mappo/curriculum.py` to refine `stage1d_single_agent_return_medium`, rename `stage1e` into a milder clutter bridge, and add `stage1f_single_agent_delivery_obstacles`; updated `train/mappo_gru.py` so promotion targets for `stage1b` through `stage1f` are more delivery-sensitive. Verification commands: `PYTHONPYCACHEPREFIX=/tmp/pycache python -m py_compile algorithms/mappo/curriculum.py train/mappo_gru.py`, `python train/train.py --backend mappo --headless --curriculum stage1 --n-agents 6 --total-steps 12288 --rollout-steps 64 --update-epochs 2 --minibatch-size 128 --eval-every 2048 --eval-episodes 2 --stage-repeat-limit 0 --no-plots --folder-name mappo_prompt31_verify`, `python train/train.py --backend mappo --headless --curriculum stage1 --n-agents 6 --total-steps 12288 --rollout-steps 64 --update-epochs 2 --minibatch-size 128 --eval-every 2048 --eval-episodes 2 --stage-repeat-limit 0 --no-plots --folder-name mappo_prompt31_verify2`, and `python train/train.py --backend mappo --headless --curriculum stage1 --n-agents 6 --total-steps 12288 --rollout-steps 64 --update-epochs 2 --minibatch-size 128 --eval-every 2048 --eval-episodes 2 --stage-repeat-limit 0 --no-plots --folder-name mappo_prompt31_verify3`. The final run completed cleanly, but `stage1d`, `stage1e`, and `stage1f` still collapsed to zero greedy delivery in the short smoke test, so prompt 31 improved the curriculum structure but did not yet solve bridge-stage return behavior.
- 2026-03-30: Added prompt 32 in `docs/prompts/32_guaranteed_post_pickup_homing_prompt.md` to address the prompt-31 outcome. Prompt 32 shifts the design focus away from mild clutter staging and toward a guaranteed post-pickup homing stage, followed by a separate mild clutter-return stage and only then a true obstacle-return stage.
- 2026-03-30: Executed prompt 32. Added stage-controlled target-to-nest distance and near-target agent spawning in `env/config.py`, `train/experiment_utils.py`, and `env/swarm_env.py`; renamed the homing lesson to `stage1d_single_agent_guaranteed_homing` and updated the early return curriculum in `algorithms/mappo/curriculum.py`; passed the new spawn controls through `train/mappo_gru.py` and tightened the guaranteed-homing delivery target. Updated `README.md`, `docs/TRAINING_MAPPO.md`, and `docs/QandA.md`.
- 2026-03-30: Verified prompt 32 with `PYTHONPYCACHEPREFIX=/tmp/pycache python -m py_compile env/config.py train/experiment_utils.py env/swarm_env.py algorithms/mappo/curriculum.py train/mappo_gru.py` and `python train/train.py --backend mappo --headless --curriculum stage1 --n-agents 6 --total-steps 12288 --rollout-steps 64 --update-epochs 2 --minibatch-size 128 --eval-every 2048 --eval-episodes 2 --stage-repeat-limit 0 --no-plots --folder-name mappo_prompt32_verify`. The run completed cleanly under `runs/mappo_prompt32_verify_20260330_215803/`: sampled behavior improved in `stage1d_single_agent_guaranteed_homing` (mean pickup `~= 2.727`, mean delivery `~= 0.273`) and `stage1e_single_agent_delivery_bridge` showed slight nonzero sampled delivery, but greedy evaluation still remained at zero pickup and zero delivery in the short verification run.
- 2026-03-30: Added prompt 33 in `docs/prompts/33_fix_sampled_to_greedy_homing_prompt.md` to address the next bottleneck after prompt 32. Prompt 33 focuses on converting sampled post-pickup homing into greedy post-pickup homing by tightening trainer pressure, entropy/determinism timing, checkpoint scoring, and delivery-sensitive promotion rather than changing stage geometry again.
- 2026-03-30: Executed prompt 33. Updated `algorithms/mappo/curriculum.py` so the return-critical stages use more aggressive entropy schedules, and updated `train/mappo_gru.py` so greedy checkpoint scoring heavily favors completed delivery and delivery conversion in those stages while stage summaries now print sampled-vs-greedy pickup/delivery gaps. Updated `README.md`, `docs/TRAINING_MAPPO.md`, and `docs/QandA.md`.
- 2026-03-30: Verified prompt 33 with `PYTHONPYCACHEPREFIX=/tmp/pycache python -m py_compile algorithms/mappo/curriculum.py train/mappo_gru.py` and `python train/train.py --backend mappo --headless --curriculum stage1 --n-agents 6 --total-steps 12288 --rollout-steps 64 --update-epochs 2 --minibatch-size 128 --eval-every 2048 --eval-episodes 2 --stage-repeat-limit 0 --no-plots --folder-name mappo_prompt33_verify`. The run completed cleanly under `runs/mappo_prompt33_verify_20260330_220317/`: sampled return behavior improved slightly in `stage1d_single_agent_guaranteed_homing` and `stage1e_single_agent_delivery_bridge`, but greedy evaluation still remained at zero pickup and zero delivery in the short verification run.
- 2026-03-30: Added prompt 34 in `docs/prompts/34_fix_pickup_without_delivery_and_hard_cap_prompt.md` based on the `mappo_full_600k_33` run. Prompt 34 targets two concrete problems shown by that run: `--total-steps` is not acting as a hard global stop, and later-stage training still treats pickup-rich / delivery-zero behavior as acceptable progress all the way into the full-swarm stages.
- 2026-03-30: Executed prompt 34. Updated `train/mappo_gru.py` so `--total-steps` is enforced as a hard global cap during rollout/stage progression, stage promotion now includes minimum delivery conversion, later swarm-stage greedy scoring penalizes pickup-without-delivery much more strongly, and stage metadata/runtime summaries now record hard-stop and conversion targets. Updated `README.md`, `docs/TRAINING_MAPPO.md`, and `docs/QandA.md`.
- 2026-03-30: Added a follow-up Q&A after prompt 34 explaining the next recommended fix for the remaining pickup-rich / delivery-zero behavior: focus on carrying-phase return-to-nest learning with stronger carrying-only nest-progress shaping, suppress exploration reward while carrying, require reliable post-pickup homing before reintroducing clutter, and consider carrying-phase anti-stuck shaping near obstacles and the nest.
- 2026-03-30: Added prompt 35 in `docs/prompts/35_fix_carrying_phase_homing_prompt.md` to implement the next recommended fix after prompt 34. Prompt 35 focuses on the carrying phase specifically: stronger carrying-only nest-progress shaping, suppression of exploration reward while carrying, carrying-phase anti-stall pressure, delivery-first checkpointing, and clearer carrying-to-delivery curriculum/metrics.
- 2026-03-30: Executed prompt 35. Updated `env/config.py`, `train/experiment_utils.py`, `env/swarm_env.py`, `algorithms/mappo/curriculum.py`, and `train/mappo_gru.py` so carrying agents now incur explicit low-progress / low-displacement penalties, the return-focused stages configure stronger carrying-phase pressure, and MAPPO episode/eval logs now record carrying-stall and carrying-penalty metrics. Updated `README.md`, `docs/TRAINING_MAPPO.md`, and `docs/QandA.md`.
- 2026-03-30: Verified prompt 35 with `PYTHONPYCACHEPREFIX=/tmp/pycache python -m py_compile env/config.py train/experiment_utils.py env/swarm_env.py algorithms/mappo/curriculum.py train/mappo_gru.py` and `python train/train.py --backend mappo --headless --curriculum stage1 --n-agents 6 --total-steps 12288 --rollout-steps 64 --update-epochs 2 --minibatch-size 128 --eval-every 2048 --eval-episodes 2 --stage-repeat-limit 0 --no-plots --folder-name mappo_prompt35_verify`. The run completed under `runs/mappo_prompt35_verify_20260330_223910/`: the new carrying metrics were informative in the harder homing stages (`stage1e` and `stage1f` showed clearly elevated carrying low-progress / low-displacement fractions and carrying penalties), but greedy evaluation still remained at zero pickup and zero delivery in the smoke run.
- 2026-03-30: Added a follow-up Q&A after prompt 35 explaining the next likely fix for greedy delivery: make the early homing stages more deterministic, gate advancement on greedy delivery rather than sampled success, reward short-horizon sustained nest-distance reduction while carrying, and consider a simpler carrying-phase control regime before restoring full obstacle complexity.
- 2026-03-30: Added prompt 36 in `docs/prompts/36_force_greedy_homing_prompt.md` to implement the next recommended fix after prompt 35. Prompt 36 focuses on converting sampled carrying-to-delivery behavior into stable greedy homing by reducing entropy in the homing stages, strengthening greedy-delivery gating, rewarding sustained nest progress while carrying, and optionally simplifying early carrying-phase control in a stage-bounded way.
- 2026-03-30: Executed prompt 36. Updated `env/config.py`, `train/experiment_utils.py`, `env/swarm_env.py`, `algorithms/mappo/curriculum.py`, and `train/mappo_gru.py` to add sustained carrying-progress shaping, lower entropy more aggressively in the guaranteed-homing / bridge / obstacle-return stages, give the bridge and obstacle-return stages slightly more budget, and tighten early return-stage delivery/conversion promotion targets. Updated `README.md`, `docs/TRAINING_MAPPO.md`, and `docs/QandA.md`.
- 2026-03-30: Verified prompt 36 with `PYTHONPYCACHEPREFIX=/tmp/pycache python -m py_compile env/config.py train/experiment_utils.py env/swarm_env.py algorithms/mappo/curriculum.py train/mappo_gru.py` and `python train/train.py --backend mappo --headless --curriculum stage1 --n-agents 6 --total-steps 12288 --rollout-steps 64 --update-epochs 2 --minibatch-size 128 --eval-every 2048 --eval-episodes 2 --stage-repeat-limit 0 --no-plots --folder-name mappo_prompt36_verify`. The run completed under `runs/mappo_prompt36_verify_20260330_224842/`: the new deterministic homing settings and sustained-progress shaping changed the stage dynamics, but the short smoke run still ended with zero greedy delivery in `stage1d`, `stage1e`, and `stage1f`.
- 2026-03-30: Added prompt 37 in `docs/prompts/37_solve_greedy_delivery_prompt.md` to push beyond reward shaping and stage tuning. Prompt 37 focuses on adding a more explicit homing-specific lesson, making the carrying-phase curriculum harder to exit without real greedy delivery, and bridging that lesson back into the normal pickup-plus-clutter task.
- 2026-03-30: Executed prompt 37. Updated `env/config.py`, `env/swarm_env.py`, `algorithms/mappo/curriculum.py`, and `train/mappo_gru.py` to add `start_carrying_food`, a new `stage1d_single_agent_carry_bootstrap` lesson, the shifted `stage1e`/`stage1f`/`stage1g` return stack, and per-stage repeat-floor overrides for the return-critical lessons. During verification, prompt 37 also exposed and fixed a real bug in `env/swarm_env.py`: the tank and hover movement drivers were dropping `carrying_food` on normal movement updates.
- 2026-03-30: Verified prompt 37 with `PYTHONPYCACHEPREFIX=/tmp/pycache python -m py_compile env/config.py env/swarm_env.py algorithms/mappo/curriculum.py train/mappo_gru.py`, `python train/train.py --backend mappo --headless --curriculum stage1 --n-agents 6 --total-steps 12288 --rollout-steps 64 --update-epochs 2 --minibatch-size 128 --eval-every 2048 --eval-episodes 2 --stage-repeat-limit 0 --no-plots --folder-name mappo_prompt37_verify`, and `python train/train.py --backend mappo --headless --curriculum stage1 --n-agents 6 --total-steps 8192 --rollout-steps 64 --update-epochs 2 --minibatch-size 128 --eval-every 2048 --eval-episodes 2 --stage-repeat-limit 0 --no-plots --folder-name mappo_prompt37_verify_fix`. The fixed run completed under `runs/mappo_prompt37_verify_fix_20260330_225802/`: `stage1d_single_agent_carry_bootstrap` produced the first nonzero greedy delivery seen in the dedicated homing lesson, `stage1e_single_agent_guaranteed_homing` also achieved nonzero greedy pickup/delivery and promoted, and `stage1f_single_agent_delivery_bridge` still collapsed back to zero greedy delivery in the short smoke run.
- 2026-03-30: Added prompt 38 in `docs/prompts/38_preserve_greedy_delivery_under_mild_clutter_prompt.md` to target the next narrow bottleneck after prompt 37. Prompt 38 focuses on preserving greedy delivery in `stage1f_single_agent_delivery_bridge` by making the bridge stage a true continuity stage from `stage1e_single_agent_guaranteed_homing`, instead of another collapse point when mild clutter is reintroduced.
- 2026-03-30: Executed prompt 38. Updated `env/config.py`, `train/experiment_utils.py`, `env/swarm_env.py`, `algorithms/mappo/curriculum.py`, and `train/mappo_gru.py` to add `target_nest_corridor_clearance`, stage-specific obstacle sizing, a smaller and more continuity-preserving `stage1f_single_agent_delivery_bridge`, and a stricter bridge promotion target. The bridge stage now tries to preserve a clear nest-to-target return corridor while still containing one real obstacle.
- 2026-03-30: Verified prompt 38 with `PYTHONPYCACHEPREFIX=/tmp/pycache python -m py_compile env/config.py train/experiment_utils.py env/swarm_env.py algorithms/mappo/curriculum.py train/mappo_gru.py`, `python train/train.py --backend mappo --headless --curriculum stage1 --n-agents 6 --total-steps 12288 --rollout-steps 64 --update-epochs 2 --minibatch-size 128 --eval-every 2048 --eval-episodes 2 --stage-repeat-limit 0 --no-plots --folder-name mappo_prompt38_verify`, and `python train/train.py --backend mappo --headless --curriculum stage1 --n-agents 6 --total-steps 20000 --rollout-steps 64 --update-epochs 2 --minibatch-size 128 --eval-every 2048 --eval-episodes 2 --stage-repeat-limit 0 --no-plots --folder-name mappo_prompt38_verify2`. The first run hit the hard cap before `stage1f`; the larger run under `runs/mappo_prompt38_verify2_20260330_231105/` reached the bridge stage and showed real improvement: `stage1f_single_agent_delivery_bridge` produced a nonzero greedy eval row (`pickup = 2.0`, `delivery = 1.5`, `conversion ~= 0.8333`) before later falling back to zero at the hard-stop eval. So prompt 38 improved bridge-stage continuity materially, but greedy bridge delivery is still not stable end-to-end in the short verification run.
- 2026-03-30: Added prompt 39 in `docs/prompts/39_stabilize_bridge_greedy_delivery_prompt.md` to target the final stage-1 blocker after prompt 38. Prompt 39 focuses on making `stage1f_single_agent_delivery_bridge` stable enough for real training by reducing within-stage greedy-delivery regression, preserving the best bridge behavior instead of drifting away from it, and keeping stage 1 from remaining the bottleneck before full curriculum runs.
- 2026-03-30: Executed prompt 39. Updated `train/mappo_gru.py` so stage-end evaluation restores the best within-stage actor/critic/optimizer state before the final greedy eval and promotion check. This is a narrow bridge-stage stabilization fix: once `stage1f_single_agent_delivery_bridge` discovers a good greedy delivery policy, the trainer no longer has to judge the stage on a later drifted policy.
- 2026-03-30: Verified prompt 39 with `PYTHONPYCACHEPREFIX=/tmp/pycache python -m py_compile train/mappo_gru.py` and `python train/train.py --backend mappo --headless --curriculum stage1 --n-agents 6 --total-steps 20000 --rollout-steps 64 --update-epochs 2 --minibatch-size 128 --eval-every 2048 --eval-episodes 2 --stage-repeat-limit 0 --no-plots --folder-name mappo_prompt39_verify`. The run completed under `runs/mappo_prompt39_verify_20260330_231951/`: `stage1d_single_agent_carry_bootstrap` and `stage1e_single_agent_guaranteed_homing` both retained nonzero greedy delivery, and `stage1f_single_agent_delivery_bridge` kept nonzero greedy delivery across all six logged eval rows, including the final eval at the hard budget cap. This is the first stage-1 result that looks stable enough to proceed to a real full training run.
- Added prompt 40 for preserving greedy delivery through the first small-swarm scale-up after the prompt-39 full run collapsed in `stage2a_small_swarm_medium`.
- Implemented prompt 40 by inserting a small-swarm bootstrap stack (`stage2a` carry bootstrap, `stage2b` easy delivery, `stage2c` delivery-first medium, `stage2d` larger small-swarm) and updating MAPPO promotion logic and docs to match.
- Added prompt 41 to preserve the solved stage-1 stack while reducing repeat/budget drain so the new small-swarm bootstrap stages are actually reached in verification.
- Implemented prompt 41 by lowering repeat pressure and budget weights for `stage1d`/`stage1e`/`stage1f` and relaxing the pure carry-bootstrap promotion target so `stage1_to_2` verification can reach the new swarm bootstrap stages.
